# 📅 일일 업무 보고 브리핑 (06월 18일 (목요일))

> 당일 팀원들의 Notion Daily Work Log 취합 요약 브리핑 히스토리입니다.

---

## 👤 김윤회 님 - 출장(스마트팜 외)

* _금일 출장(스마트팜 외)입니다._


---

## 👤 김희승 님

* **[리팩토링](https://www.notion.so/리팩토링-383c640ccca4808a9d20d72dfdca45e8)**
  - (상세: code_review.md 지적 사항 수정 — sensor_manager.c 역방향 include 제거, register_map.c 락 없는 fallback 제거, staging 직접 인덱싱 → API 교체, tempSensors)
  - (상세: 하드코딩 제거, 중복 include/인라인 extern 제거)
  - (상세: heartbeat 패턴 수정 — ReadStaging API 추가했다가 "staging은 쓰기 전용" 원칙 확인 후 제거, static 변수로 교체)
  - (상세: sensor_configs.c Led_Phase1_Blink 제거 — 콜백 주입으로 L1 HAL 코드를 composition root에서 분리)
  - (상세: 1-Wire 프로토콜 바이트 캡슐화 — 0xCC/0x44/0xBE를 driver_onewire.c로 이동, OneWireDriver_StartConversionAll + ReadScratchpadByRom 신규 구현)
  - (상세: 멀티 디바이스 지원 + ROM 코드 관리 — board_config에 모드 스위치/ROM 코드/버스 인덱스 추가, 베어메탈 테스트 메뉴에 ROM 리더 추가)
  - (상세: rawData 단위 계약 위반 3개 수정 — Phase 1 hydraulic, soil moisture ReadRegister, RTOS 캡처 레이블)
  - (상세: ReadRaw/Process 계약 복구 — live-zero 감지를 ReadRaw에서 ProcessOps로 되돌림)
  - (상세: DiagnoseOps/DiagnosticTask 완전 제거 — 292줄 삭제, diagStatus 소유권 SystemMonitorTask 단일화, 비트 이름 errCnt 1:1 정렬 (FLOATING→COMM_ERROR 등),)
  - (상세: BOOT_UNREADY/SAMPLE_TIMEOUT SystemMonitorTask 이관)
  - (상세: 빌드 에러 수정 — derived 선언 누락 (hydraulic, soil_moisture IsReadyPhase2Ops))
  - (상세: 주요 아키텍처 결정:)
  - (상세: 현상(errCnt) ≠ 추론(diagStatus) 원칙 확립, DiagnoseOps가 이를 위반함을 확인)
  - (상세: errCnt 비트 1:1 정렬 + monitorErrThreshold UNUSED 메커니즘으로 추가 코드 없이 의미 기반 매핑 완성)


---

## 👤 최현빈 님

* **[PLC 자동화 서비스 저장 조건 분기 및 조건 중복 실행 방지](https://www.notion.so/plc-자동화-서비스-저장-조건-분기-및-조건-중복-실행-방지-383c640ccca4800cab1bed0a9639e126)**
  - (상세: 개요: 로컬 PLC 엣지 환경에서 구동되는 로직의 저장 정책을 조율하고, 백엔드 사양에 맞춰 수동 루틴 무시, 자동화 중복 조건 평가 차단 및 JSON 데이터 역직렬화 예외 문제를 해결함.)
  - (상세: 주요 진행 내용:)
  - (상세: 자동 실행 루틴 선별 적재:)
  - (상세: app/service/handler.py: 자동화 이벤트 핸들러가 가동될 때 payload.type == 'auto'인 자동 규칙만 디스크 DB에 upsert하여 기록하도록 제한하고 수동은 무시하도록 정리함.)
  - (상세: IfAction 조건 중복 스킵 로직 연동:)
  - (상세: app/service/automation/models.py: 백엔드 및 가상 PLC 환경과 규칙을 일원화하기 위해 조건 분석 객체인 IfAction 내 prev_cond 플래그를 동일 도입하여, 직전 평가와 상태가 같다면 하위 동작 가동을 취소하고 건너뛰도록 리팩토링함.)
  - (상세: 데이터베이스 로드 시 JSON 문자열 파싱 핫픽스:)
  - (상세: main.py: SQLite에 보관되었던 기존 자동화 상태를 로드할 때 condition 데이터가 string 타입으로 반환되는 에러를 해결하고자, json.loads를 적용하여 정상 딕셔너리 포맷으로 Pydantic DTO 바인딩이 이루어지게 수정함.)

* **[종료 루틴(elseActions) 관리 UI 구현 및 자동화 설정 입력값 보정](https://www.notion.so/종료-루틴elseactions-관리-ui-구현-및-자동화-설정-입력값-보정-383c640ccca4806dac1be7c614b1320f)**
  - (상세: 개요: 자동화 설정 조건이 맞지 않을 때 동작할 종료 루틴(elseActions)을 설정, 저장 및 뷰어 화면에 렌더링하는 전반적 흐름을 개발하고, 임계치 및 지연 시간 타이핑 시 발생하던 입력 초기화 결함과 기기 내역 날짜 출력 오류를 해결함.)
  - (상세: 주요 진행 내용:)
  - (상세: 종료 루틴(elseActions) 제어 및 복원 체계 마련:)
  - (상세: src/components/automation/AutomationBuilder.tsx: 사용자가 조건 미충족 시 수행할 종료 동작(elseActions)을 UI 단에서 추가, 배치 이동, 삭제 가능하도록 편집기 영역을 확장함.)
  - (상세: src/components/automation/AddActionModal.tsx: 조건 외 동작 설정에 대응하는 액션 추가 모달 레이아웃을 구현함.)
  - (상세: src/components/automation/RoutineDetailOverlay.tsx: 루틴 정보 팝업에 "어떻게 종료 할까요?" 단락을 별도 설계하고, 복원된 elseActions 데이터를 일목요연하게 렌더링하도록 UI를 추가함.)
  - (상세: src/utils/automationMapper.ts: 서버의 DTO 데이터와 프론트 Routine 형식을 치환해 주는 디코더(decodeAutomationRule) 로직 내에 if 문 내부 else 속성을 elseActions 배열로 복원하는 디코딩 사양을 고도화하고, 대기 단위(Minute/Second) 포맷 매핑 텍스트 픽스를 적용함.)
  - (상세: src/types/index.ts: 공통 Routine 스펙에 elseActions 컬럼을 선언하고 빌더 내 Condition, Action 인터페이스 정의 추가.)
  - (상세: 입력 도중 인풋 박스 0 초기화 현상 개선:)
  - (상세: src/components/automation/AutomationBuilder.tsx: 사용자가 센서 임계치나 대기 시간을 키보드로 입력하던 과정에서 백스페이스 입력 등으로 인해 빈칸이 생겼을 때 자동으로 숫자 0이 인풋 창에 삽입되어 타이핑 흐름을 끊던 버그를 공백 문자열('') 임시 허용 방식으로 개선하고, 최종 폼 전송 시점에만 0으로 치환해 송신하게끔 예외를 구축함.)
  - (상세: 상태 변경 성공 토스트 피드백 연동:)
  - (상세: src/page/AutomationPage.tsx: 수동/자동 루틴 활성화 및 비활성화 상태가 정상 반영되었을 때 화면 하단에 가시적인 파란색 스타일(bg-[#139FF8])의 바운싱 토스트 알림을 1.5초간 노출하도록 처리하여 UX 만족도를 강화함.)
  - (상세: 기기 이력 모달 날짜 오염 버그 픽스:)
  - (상세: src/components/DeviceDetailModal.tsx: 기기 제어 상세 이력의 일시 변환 헬퍼 함수에서 월(getMonth() + 1) 처리 오류와 요일(getDay())로 날짜를 표기하던 인쇄 버그를 일(getDate())로 수정하여 정확한 타임스탬프를 표시함.)
  - (상세: 단위 테스트 시나리오 보강:)
  - (상세: tests/components/automation/...: 대기 지연(초/분) 인코딩 테스트, 비어있는 센서 임계값 저장 시 0 변환 테스트, RoutineCard 클릭 시 onToggle 핸들러 정상 트리거 여부 등을 수립하여 자동화 기능 배포 안정성을 확보함.)

* **[RxJS EmptyError 타이밍 불일치 이슈 해결 및 자동화 DTO 처리 개선](https://www.notion.so/rxjs-emptyerror-타이밍-불일치-이슈-해결-및-자동화-dto-처리-개선-383c640ccca480f5ab4acf853a20aa14)**
  - (상세: 개요: RabbitMQ 메시지 발행 후 Ack 응답을 수신하는 과정에서 RxJS 구독 시점의 타이밍 이슈로 인해 발생하는 EmptyError: no elements in sequence 에러를 해결하고, 수동 루틴 등 엔티티 저장 로직의 동작 조건을 보완함.)
  - (상세: 주요 진행 내용:)
  - (상세: ReplaySubject 도입을 통한 메시지 구독 누락 방지:)
  - (상세: src/modules/rabbitmq/rabbitmq.service.ts: 기존 응답 처리 대기 방식의 한계로 인해 이미 결과가 도착한 뒤 구독이 시작될 경우 에러가 나는 현상을 방지하고자, 메시지 발행 직후 마지막 값을 캐싱하여 늦게 시작된 구독자에게도 정상 전달할 수 있도록 Subject에서 ReplaySubject(1)로 교체함.)
  - (상세: 메시지 발행 및 구독 로직 시퀀스 개선:)
  - (상세: src/modules/api/automation/automation.service.ts: 메시지를 RabbitMQ로 실제 전송(publish)하기 전에 수신 대기용 Observable을 먼저 초기화(createObservable)하도록 발행 순서를 조정하여 데이터 소실을 방지하고, 루틴 유형이 AUTO인 경우에만 엔티티 상태 저장(repository.save)을 처리하도록 조건을 고도화함.)
  - (상세: 이슈 해결 설계 문서 작성:)
  - (상세: docs/superpowers/plans/2026-06-18-rabbitmq-empty-error-fix.md: 해당 트러블슈팅의 구체적 원인 규명 및 RxJS ReplaySubject 적용을 위한 구현 단계 정의 문서를 신규 추가함.)


---

## 📢 06월 18일 (목요일) 업무 요약 브리핑

> 각 팀원들의 금일 업무 목적 및 진행 의미 요약입니다.

### 👤 김희승 님
* **리팩토링**: code_review.md 지적 사항 수정 — sensor_manager.c 역방향 include 제거, register_map.c 락 없는 fallback 제거, staging 직접 인덱싱 → API 교체, tempSensors

### 👤 최현빈 님
* **PLC 자동화 서비스 저장 조건 분기 및 조건 중복 실행 방지**: 로컬 PLC 엣지 환경에서 구동되는 로직의 저장 정책을 조율하고, 백엔드 사양에 맞춰 수동 루틴 무시, 자동화 중복 조건 평가 차단 및 JSON 데이터 역직렬화 예외 문제를 해결함.
* **종료 루틴(elseActions) 관리 UI 구현 및 자동화 설정 입력값 보정**: 자동화 설정 조건이 맞지 않을 때 동작할 종료 루틴(elseActions)을 설정, 저장 및 뷰어 화면에 렌더링하는 전반적 흐름을 개발하고, 임계치 및 지연 시간 타이핑 시 발생하던 입력 초기화 결함과 기기 내역 날짜 출력 오류를 해결함.
* **RxJS EmptyError 타이밍 불일치 이슈 해결 및 자동화 DTO 처리 개선**: RabbitMQ 메시지 발행 후 Ack 응답을 수신하는 과정에서 RxJS 구독 시점의 타이밍 이슈로 인해 발생하는 EmptyError: no elements in sequence 에러를 해결하고, 수동 루틴 등 엔티티 저장 로직의 동작 조건을 보완함.

