# 📅 [7월 2주차] 주간 업무 브리핑
> 월요일 출근 시간 전, 지난주 업무 분석 및 금주 계획 종합 브리핑입니다.

---

## 📂 1. 지난주 실제 업무 보고 (Daily Work Log) (7/6~7/12)

### ■ 07월 06일 (월요일)
* **[/camera/yolo/processed](https://www.notion.so/camerayoloprocessed-395c640ccca480ebafd6e0378ce9720d)**
  * (상세: 개요: 컨테이너 내부에서 color/depth 수신과 processed publish는 성공했지만, RViz2에서 이미지가 보이지 않는 문제를 QoS와 처리 속도 관점에서 점검함.)
  * (상세: 주요 진행 내용:)
  * (상세: RViz2 QoS 확인:)
  * (상세: processed publisher를 reliable로 변경해 RViz2 기본 reliable 구독과 맞추는 방향을 먼저 검토함.)
  * (상세: 이후 큰 이미지 스트림에서는 reliable backpressure가 성능에 불리할 수 있어, processed publisher를 sensor_data QoS로 되돌리고 RViz2 Image display의 Reliability를 Best Effort로 맞추는 방향으로 정리함.)
  * (상세: 처리 루프 병목 완화:)
  * (상세: 기존 구조는 color callback 안에서 YOLO 추론을 바로 수행해 추론 시간이 길면 executor callback 처리 자체가 밀리는 구조였음.)
  * (상세: color callback은 최신 프레임만 저장하고, 별도 worker thread가 최신 color/depth 프레임을 가져와 추론 및 publish하도록 변경함.)
  * (상세: 오래된 프레임 backlog를 쌓지 않고 최신 프레임 기준으로 처리해 RViz2 출력 지연을 줄이는 방향으로 정리함.)
  * (상세: CUDA 미사용 원인 확인:)
  * (상세: torch.cuda.is_available()가 False이고, The NVIDIA driver on your system is too old (found version 12060) 경고가 발생하는 것을 확인함.)
  * (상세: 현재 PyTorch CUDA 빌드가 호스트 Jetson 드라이버/CUDA 런타임보다 높은 버전을 기대해 CUDA 초기화가 실패하고 있다고 판단함.)
  * (상세: YOLOv7이 CPU로 실행되기 때문에 /camera/yolo/processed가 약 0.4 Hz 수준으로 떨어지는 것이 현재 성능 병목의 직접 원인으로 정리됨.)
* **[CUDA 컨테이너 내부 ROS2 토픽 수신 문제 분리](https://www.notion.so/cuda-컨테이너-내부-ros2-토픽-수신-문제-분리-395c640ccca4803f8a48fb2e16fc0395)**
  * (상세: 개요: 호스트에서는 RealSense ROS2 토픽이 정상 출력되지만 CUDA 컨테이너 내부 노드에서 메시지를 받지 못하는 문제를 ROS2 graph, QoS, DDS data plane 순서로 분리함.)
  * (상세: 주요 진행 내용:)
  * (상세: 노드 진단 로그 보강:)
  * (상세: 실행 중인 코드 경로, RMW implementation, 구독 토픽, 발견된 publisher 수와 type/QoS를 로그로 출력하도록 추가함.)
  * (상세: create_subscription() 반환 객체가 GC로 정리되지 않도록 subscription과 timer를 멤버 변수로 보관함.)
  * (상세: color/depth 각각에 best-effort 계열과 reliable 구독을 모두 붙여 QoS mismatch 가능성을 점검함.)
  * (상세: raw subscription 카운터를 추가해 typed callback 이전에 DDS payload 자체가 들어오는지 확인할 수 있게 함.)
  * (상세: 컨테이너 통신 문제 재분리:)
  * (상세: 컨테이너 안 ros2 topic list/info에서는 토픽이 보이지만 ros2 topic echo가 안 되는 상태를 확인해, 애플리케이션 코드보다 DDS data plane 문제가 우선이라고 판단함.)
  * (상세: -net=host만으로는 discovery가 보여도 데이터 전달이 막힐 수 있어 -ipc=host 또는 Fast DDS shared memory 우회가 필요할 수 있다고 정리함.)
  * (상세: 기존 컨테이너 재설치 없이 FASTDDS_BUILTIN_TRANSPORTS=UDPv4로 shared memory 경로를 우회하는 테스트 방향을 제시함.)
  * (상세: 컨테이너 실행/마운트 정리:)
  * (상세: docker run에서 v 옵션은 이미지 이름 앞에 배치해야 하며, 이미지 이름 뒤에 두면 컨테이너 내부 명령으로 전달되어 exec: -v: invalid option 오류가 발생하는 것을 정리함.)
  * (상세: /tmp/.X11-unix 마운트는 GUI 표시용 X11 socket 공유일 뿐이며, ROS workspace 공유를 위해서는 별도 v /home/agv/agv-agent/ros2_ws:/ros2_ws가 필요하다고 정리함.)
  * (상세: CUDA 컨테이너 안에서 ROS2 Humble deb 설치를 진행할 경우, 먼저 apt-get update와 Ubuntu codename 확인이 필요하고, Humble은 Ubuntu 22.04 jammy 기준임을 확인해야 한다고 정리함.)
* **[cv_bridge](https://www.notion.so/cvbridge-395c640ccca480c98496f163dfb221c4)**
  * (상세: 개요: Jetson/ROS 환경에서 NumPy 1.x를 유지해야 하는 조건에 맞춰 cv_bridge와 OpenCV 의존성을 재검토함.)
  * (상세: 주요 진행 내용:)
  * (상세: cv_bridge 의존성 제거:)
  * (상세: CvBridge가 수행하던 sensor_msgs/Image와 OpenCV/NumPy 배열 간 변환을 직접 구현함.)
  * (상세: color/depth 토픽에서 필요한 encoding 범위만 처리하도록 제한해 의존성을 줄임.)
  * (상세: package.xml에서 cv_bridge 의존성을 제거함.)
  * (상세: OpenCV 버전 조정:)
  * (상세: opencv-python==5.0.0.93은 현재 Python 조건에서 NumPy 2.x 계열을 요구하는 문제가 있어 NumPy 1.x 유지 조건과 충돌한다고 판단함.)
  * (상세: requirements.txt, pyproject.toml에 numpy>=1.23.5,<2와 opencv-python==4.11.0.86을 반영함.)
* **[RealSense SDK 직접 캡처 제거 및 ROS2 토픽 기반 처리 노드 전환](https://www.notion.so/realsense-sdk-직접-캡처-제거-및-ros2-토픽-기반-처리-노드-전환-395c640ccca48037bf88d2b134d3f662)**
  * (상세: 개요: 기존에 pyrealsense2 SDK를 직접 사용해 컬러/depth 프레임을 받아오던 구조를 제거하고, ROS2 이미지 토픽을 구독해 YOLOv7 추론을 수행하는 노드 구조로 전환함.)
  * (상세: 주요 진행 내용:)
  * (상세: ROS2 이미지 구독 구조 작성:)
  * (상세: color 입력 토픽을 /camera/camera/color/image_raw로 고정하고, aligned depth 입력 토픽을 /camera/camera/aligned_depth_to_color/image_raw로 구성함.)
  * (상세: sensor_msgs/msg/Image를 직접 NumPy 배열로 변환하도록 구현해 cv_bridge 의존성을 제거함.)
  * (상세: color 이미지는 896x504 크기만 처리하고, 예상 크기와 다르면 경고 로그 후 건너뛰도록 처리함.)
  * (상세: RealSense SDK 의존성 제거:)
  * (상세: realsense_yolov7/realsense_stream.py를 제거하고, 실행 흐름을 rclpy 기반 노드로 이동함.)
  * (상세: examples/, typings/pyrealsense2/, RealSense SDK 관련 Docker/compose 설정을 삭제함.)
  * (상세: pyrealsense2 없이 ROS2 wrapper가 발행하는 토픽을 입력으로 받는 방향으로 정리함.)
  * (상세: YOLOv7 추론 코드 정리:)
  * (상세: utils/는 추론에 필요한 letterbox, check_img_size, non_max_suppression, scale_coords, plot_one_box, select_device, TracedModel 중심으로 줄임.)
  * (상세: 사용하지 않는 훈련, cloud, wandb, metrics, typing 관련 파일을 제거해 런타임 표면을 줄임.)
  * (상세: YOLO weight 로딩, 더미 color/depth 입력 기반 annotate() 호출까지 확인해 최소 추론 경로가 유지되는지 점검함.)

### ■ 07월 07일 (화요일)
* **[TensorRT 적용 후 처리량 확인](https://www.notion.so/tensorrt-적용-후-처리량-확인-396c640ccca4804dbcf2ff7a73ba882d)**
  * (상세: 개요: TensorRT backend 적용 후 /camera/yolo/processed publish 속도가 안정적으로 개선되는 것을 확인함.)
  * (상세: 주요 진행 내용:)
  * (상세: 최종 측정 결과:)
  * (상세: TensorRT 적용 전 PyTorch 기준 inference는 약 150~170ms, 전체 처리 시간은 160~180ms 수준으로 측정됨.)
  * (상세: TensorRT 적용 후 /camera/yolo/processed가 약 16 FPS로 안정적으로 발행되는 상태까지 개선됨.)
  * (상세: 목표 대비 상태 정리:)
  * (상세: 목표치인 20 FPS 이상에는 아직 도달하지 못함.)
  * (상세: 현재 구조는 모델 forward만 TensorRT로 옮긴 상태이며, NMS와 draw/depth median 처리는 기존 Python/Torch 경로에 남아 있음.)
  * (상세: 다음 단계 병목은 TensorRT 적용 후 profile 로그의 nms, draw, publish, total 값을 기준으로 다시 확인해야 함.)
  * (상세: 추가 최적화 후보:)
  * (상세: yolov7-tiny 등 더 작은 모델 검토.)
  * (상세: -img-size 추가 축소와 정확도 저하 범위 비교.)
  * (상세: TensorRT engine 내부에 EfficientNMS 포함 여부 검토.)
  * (상세: FP16/INT8 최적화와 calibration 가능성 검토.)
* **[YOLOv7 TensorRT 최적화 경로 추가 및 실행 문제 해결](https://www.notion.so/yolov7-tensorrt-최적화-경로-추가-및-실행-문제-해결-396c640ccca48016ad91f672837a8bc7)**
  * (상세: 개요: PyTorch inference 병목을 줄이기 위해 기존 PyTorch backend는 유지하고, -engine 옵션을 주면 TensorRT engine을 사용하는 선택형 backend를 추가함.)
  * (상세: 주요 진행 내용:)
  * (상세: TensorRT 변환 스크립트 추가:)
  * (상세: export_tensorrt.py를 추가해 .pt -> .onnx -> .engine 변환 흐름을 구성함.)
  * (상세: trtexec를 사용해 TensorRT engine을 생성하도록 구성하고, CMake install 대상에도 변환 스크립트를 포함함.)
  * (상세: engine은 입력 크기에 묶이므로 -img-size 416으로 만든 engine은 실행 시에도 동일한 -img-size 416을 사용해야 한다고 정리함.)
  * (상세: TensorRT runtime backend 추가:)
  * (상세: realsense_yolov7/tensorrt_detector.py를 추가함.)
  * (상세: -engine models/weight/yolov7.416.engine 옵션이 주어지면 TensorRT backend를 사용하고, 옵션이 없으면 기존 PyTorch backend를 그대로 사용하도록 구성함.)
  * (상세: TensorRT는 모델 forward만 담당하고, NMS와 bbox/depth draw는 기존 Python/Torch 후처리 경로를 유지함.)
  * (상세: CUDA Python 의존성 확인:)
  * (상세: trtexec는 /opt/tensorrt/bin/trtexec에 존재하지만, Python runtime에서 from cuda import cudart가 실패하는 것을 확인함.)
  * (상세: TensorRT engine을 Python에서 실행하려면 cuda-python 패키지가 필요하므로 cuda-python>=12,<13 설치가 필요하다고 정리함.)
  * (상세: engine 생성 상태 확인:)
  * (상세: trtexec 실행 중 [TRT] Local timing cache in use. Profiling results in this builder pass will not be stored 로그에서 멈춘 것처럼 보였지만, 프로세스가 CPU를 사용하고 있어 engine 빌드 중인 상태로 판단함.)
  * (상세: ps, top, ls -lh로 trtexec 프로세스와 .engine 파일 생성 여부를 확인하는 방식으로 점검함.)
  * (상세: TensorRT 입력 shape 문제 해결:)
  * (상세: TensorRT engine은 (1, 3, 416, 416) 고정 입력인데, 기존 letterbox() 기본값 auto=True 때문에 실제 입력이 (1, 3, 256, 416)으로 만들어지는 문제를 확인함.)
  * (상세: TensorRT 경로에서는 letterbox(frame, self.img_size, auto=False, stride=self.stride)를 사용하도록 수정해 항상 engine 입력 shape와 맞는 정사각 입력을 생성하게 함.)
  * (상세: 해당 수정 후 engine을 다시 만들 필요 없이 기존 yolov7.416.engine을 그대로 사용할 수 있게 됨.)
* **[처리 병목 확인을 위한 프로파일링 옵션 추가](https://www.notion.so/처리-병목-확인을-위한-프로파일링-옵션-추가-396c640ccca480b1bc75e34c83733c56)**
  * (상세: 개요: 목표 처리량 대비 실제 FPS가 낮은 원인을 추론, 전처리, 후처리, publish 단계별로 나누기 위해 선택형 프로파일링 로그를 추가함.)
  * (상세: 주요 진행 내용:)
  * (상세: CLI 옵션 추가:)
  * (상세: -profile-every N 옵션을 추가해 N프레임마다 단계별 처리 시간을 로그로 출력할 수 있게 함.)
  * (상세: 기본값은 0으로 두어 평상시 실행에는 프로파일링 비용이 없도록 처리함.)
  * (상세: 단계별 시간 측정 추가:)
  * (상세: color/depth 변환, letterbox 전처리, NumPy to Tensor 변환, model inference, NMS, bbox/depth draw, ROS publish 시간을 분리해 측정함.)
  * (상세: CUDA 연산은 비동기 실행되므로, 정확한 측정을 위해 프로파일링이 켜진 경우에만 torch.cuda.synchronize()를 호출하도록 처리함.)
  * (상세: 측정 결과 확인:)
  * (상세: PyTorch 실행 기준 infer=150~170ms, total=160~180ms 수준으로 측정됨.)
  * (상세: 전체 처리 시간의 대부분이 모델 forward에 집중되어 있어 ROS publish, 이미지 변환, draw 최적화만으로는 목표 FPS 달성이 어렵다고 판단함.)
  * (상세: 입력 크기 조정 실험:)
  * (상세: -img-size를 낮추면 초당 약 4프레임 정도 개선되는 것을 확인함.)
  * (상세: 다만 입력 크기 축소만으로는 목표치인 20 FPS 이상에 도달하기 어려워 TensorRT 최적화를 추가 검토함.)
* **[디버깅용 구독과 로그 제거](https://www.notion.so/디버깅용-구독과-로그-제거-396c640ccca480959c61dbdf6db3f578)**
  * (상세: 요: ROS2 토픽 수신 문제가 해결된 뒤, 원인 분리를 위해 추가했던 진단용 코드와 중복 구독을 제거해 실행 코드를 단순화함.)
  * (상세: 주요 진행 내용:)
  * (상세: 디버깅 코드 제거:)
  * (상세: raw subscription 진단 코드, RMW implementation 출력, ROS graph publisher introspection 로그를 제거함.)
  * (상세: first-frame 확인 로그와 주기적인 수신 상태 로그도 제거해 실제 운용 중 불필요한 출력이 나오지 않도록 정리함.)
  * (상세: 구독 QoS 단순화:)
  * (상세: color/depth 토픽에 best-effort와 reliable을 동시에 붙이던 임시 구조를 제거함.)
  * (상세: color/depth subscription은 각각 하나씩만 유지하고, ROS2 기본 QoS depth 값을 사용하도록 단순화함.)
  * (상세: 출력 QoS 유지:)
  * (상세: /camera/yolo/processed publisher는 이미지 스트림 특성을 고려해 sensor_data QoS를 유지함.)
  * (상세: RViz2에서 볼 때는 Image display의 Reliability를 Best Effort로 맞추는 방향을 유지함.)
* **[NVIDIA PyTorch iGPU 컨테이너와 ROS 배포판 호환성 정리](https://www.notion.so/nvidia-pytorch-igpu-컨테이너와-ros-배포판-호환성-정리-396c640ccca4809cb485eb80b02bbe52)**
  * (상세: 개요: TensorRT/PyTorch가 포함된 NVIDIA PyTorch iGPU 컨테이너를 기준으로 ROS2 환경을 맞추는 과정에서 OS와 ROS 배포판 호환 문제를 분리함.)
  * (상세: 주요 진행 내용:)
  * (상세: ROS Humble 설치 실패 원인 확인:)
  * (상세: nvcr.io/nvidia/pytorch:25.06-py3-igpu 이미지에서 ros-humble-desktop 설치 시 unable to locate package 오류가 발생함.)
  * (상세: 해당 이미지 OS가 Ubuntu 24.04로 잡히기 때문에 Ubuntu 22.04 대상인 ROS Humble apt 패키지가 존재하지 않는 것이 원인으로 정리됨.)
  * (상세: Ubuntu 24.04에서는 ROS Jazzy를 설치하는 방향으로 전환함.)
  * (상세: ROS Jazzy 설치 후 토픽 수신 확인:)
  * (상세: 컨테이너 안에 ROS Jazzy를 설치한 뒤 RealSense color/depth 토픽이 ros2 topic echo로 출력되는 것을 확인함.)
  * (상세: 기존 애플리케이션 코드의 토픽 구독 문제는 컨테이너/DDS 경계 문제에서 해결된 상태로 판단함.)
  * (상세: Humble/Jazzy 혼용 문제 확인:)
  * (상세: 호스트 Humble 환경에서 컨테이너 Jazzy가 발행한 토픽을 확인할 때 ResponseError("unknown tag 'rclpy.type_hash.TypeHash'") 오류가 발생함.)
  * (상세: 이는 호스트와 컨테이너의 ROS2 배포판이 달라 CLI/type support 호환이 맞지 않는 문제로 보고, 토픽 확인과 디버깅은 컨테이너 안의 Jazzy CLI 기준으로 수행하는 방향으로 정리함.)
  * (상세: Python 실행환경 분리:)
  * (상세: PyTorch iGPU 이미지의 시스템 Python에는 torch가 있지만, uv run 또는 기존 .venv로 실행하면 No module named 'torch'가 발생할 수 있음을 확인함.)
  * (상세: Jetson 컨테이너에서는 당분간 uv run 대신 source /opt/ros/jazzy/setup.bash 후 python3 main.py로 실행하는 방향으로 정리함.)
* **[CUDA 컨테이너 내 PyTorch 실행환경 문제 분리](https://www.notion.so/cuda-컨테이너-내-pytorch-실행환경-문제-분리-396c640ccca4801cac8ddd867f14dba5)**
  * (상세: 개요: ROS2 토픽 기반 YOLOv7 노드는 동작 가능한 상태까지 올라왔지만, 처리 속도가 낮게 나오는 원인을 CUDA/PyTorch 실행환경 관점에서 분리함.)
  * (상세: 주요 진행 내용:)
  * (상세: CUDA 미사용 원인 확인:)
  * (상세: 컨테이너 안에서 torch.cuda.is_available()가 False로 나오고, The NVIDIA driver on your system is too old (found version 12060) 경고가 발생하는 것을 확인함.)
  * (상세: 당시 설치된 PyTorch가 호스트 Jetson 드라이버보다 더 높은 CUDA 빌드를 기대해 CUDA 초기화가 실패하고 있었음.)
  * (상세: 이 상태에서는 YOLOv7이 CPU로 실행되어 /camera/yolo/processed publish 속도가 약 0.4 Hz 수준으로 떨어지는 것이 정상 범위라고 판단함.)
  * (상세: Orin GPU 아키텍처 미지원 문제 확인:)
  * (상세: 이후 CUDA는 잡히지만 no kernel image is available for execution on the device 오류가 발생하는 상태를 확인함.)
  * (상세: torch.__version__, torch.version.cuda, torch.cuda.get_arch_list() 확인 결과:)
  * (상세: torch: 2.11.0+cu126)
  * (상세: CUDA: 12.6)
  * (상세: 지원 arch: ['sm_80', 'sm_90'])
  * (상세: Jetson Orin은 sm_87이 필요하므로, CUDA 버전은 맞더라도 해당 PyTorch wheel에 Orin용 kernel image가 없어 실행 실패한다고 정리함.)
  * (상세: JetPack/L4T 버전 확인:)
  * (상세: /etc/nv_tegra_release 기준 Jetson Linux R36.4.7 환경임을 확인함.)
  * (상세: PyTorch 2.11.0 일반 wheel을 유지하는 방향은 현재 Jetson Orin 환경과 맞지 않고, Jetson/Orin용 PyTorch 빌드 또는 NVIDIA iGPU PyTorch 컨테이너 사용이 필요하다고 판단함.)

### ■ 07월 08일 (수요일)
* **[독립 YOLOv7 + ByteTrack 실험 레포 구성](https://www.notion.so/독립-yolov7-bytetrack-실험-레포-구성-397c640ccca48080bf4cc932eb983b07)**
  * (상세: 개요: realsense-track 저장소를 별도로 구성해 YOLOv7 + ByteTrack 영상 추적 실험을 독립적으로 돌릴 수 있게 정리함.)
  * (상세: 주요 진행 내용:)
  * (상세: 초기 프로젝트 구성:)
  * (상세: main.py, tracker/, yolov7/, videos/palace.mp4, pyproject.toml, uv.lock 등을 포함한 첫 커밋을 생성함.)
  * (상세: ByteTrack tracker 코드와 YOLOv7 로더 코드를 프로젝트 내부에 포함해 외부 repo import 없이 실행 가능한 형태로 구성함.)
  * (상세: 기본 샘플 비디오를 대상으로 사람 detection과 tracking 결과를 확인하는 데모 흐름을 작성함.)
  * (상세: CUDA 지원 PyTorch 설치를 위한 Python 환경 조정:)
  * (상세: CUDA 지원 PyTorch 설치를 위해 .python-version을 변경하고 lockfile을 갱신함.)
  * (상세: pyproject.toml 의존성에서 별도 numpy 명시를 제거하고, torch, torchvision, opencv-python, lap 중심으로 정리함.)
  * (상세: YOLOv7 detection threshold와 NMS IoU threshold를 조정해 추적 실험 조건을 변경함.)
  * (상세: 영상 출력과 재생 속도 보정:)
  * (상세: VideoWriter를 활성화해 추적 결과 영상을 파일로 저장하도록 수정함.)
  * (상세: 원본 비디오 FPS를 기준으로 frame_interval과 waitKey delay를 계산해 imshow가 처리 속도대로 과속 재생되지 않게 보정함.)
  * (상세: 실행 중 min/max/average FPS를 출력해 처리 속도를 확인할 수 있게 함.)
  * (상세: 디버그/검증 기능 추가:)
  * (상세: -detection-only 옵션을 추가해 tracker 없이 YOLOv7 bbox와 score만 표시할 수 있게 함.)
  * (상세: -debug-association 옵션을 추가해 association 비용 분석 로그를 실행 옵션으로 켤 수 있게 함.)
  * (상세: track_thresh, track_buffer, match_thresh, appearance 관련 값을 실험 목적에 맞게 조정함.)
  * (상세: 반복 재생과 track id 초기화 처리:)
  * (상세: -loop 옵션을 추가해 입력 영상이 끝나면 처음으로 되돌아가 반복 재생할 수 있게 함.)
  * (상세: 반복 재생 시 tracker 내부 상태와 전역 track id count를 초기화하는 clear() 경로를 추가함.)
  * (상세: 생성된 mp4 결과물은 .gitignore에 추가해 이후 실험 출력물이 git에 포함되지 않도록 정리함.)
* **[업데이트 로직 분석 및 association 개선 실험](https://www.notion.so/업데이트-로직-분석-및-association-개선-실험-397c640ccca4808b97a1d64dc2c1f2f6)**
  * (상세: 개요: ByteTrack의 ID 유지가 단순 Kalman filter만으로 동작하는 것이 아니라, detection 결과와 IoU association을 중심으로 동작한다는 점을 코드 기준으로 분석하고, ID switch 완화를 위한 appearance cost를 추가함.)
  * (상세: 주요 진행 내용:)
  * (상세: BYTETracker.update() 단계별 동작 분석:)
  * (상세: detector 결과를 high score detection과 low score detection으로 나누는 흐름을 확인함.)
  * (상세: 기존 tracked/lost track의 위치를 Kalman filter로 예측한 뒤, high score detection과 1차 IoU matching을 수행하는 구조를 정리함.)
  * (상세: 1차 matching에 실패한 track은 low score detection과 2차 matching을 수행하며, 이것이 ByteTrack의 핵심 아이디어라고 정리함.)
  * (상세: 매칭 실패 track은 바로 제거하지 않고 lost 상태로 보관한 뒤 track_buffer 기간이 지나면 removed 처리되는 흐름을 확인함.)
  * (상세: RGB 값을 Kalman filter에 넣는 방안 검토:)
  * (상세: Kalman filter 상태 벡터는 x, y, a, h, vx, vy, va, vh로 위치, 크기, 속도 모델에 맞춰져 있음을 확인함.)
  * (상세: bbox 내부 RGB를 Kalman 상태에 직접 넣으면 운동 모델과 맞지 않아 위치 추정까지 오염될 수 있다고 판단함.)
  * (상세: RGB/appearance 정보는 Kalman filter가 아니라 matching cost에 섞는 방식이 더 적절하다고 정리함.)
  * (상세: 색 히스토그램 기반 appearance cost 추가:)
  * (상세: bbox crop에서 3채널 색 히스토그램을 만들고, STrack에 appearance feature를 저장하도록 수정함.)
  * (상세: 매칭 성공 시 track appearance를 EMA 방식으로 업데이트하도록 구현함.)
  * (상세: 기존 IoU/score 기반 cost에 appearance distance를 가중합으로 섞는 fuse_appearance() 경로를 추가함.)
  * (상세: 데모 기준 appearance_weight, appearance_momentum 값을 두고 큰 bbox에서는 ID 유지 성능이 일부 개선되는 것을 확인함.)
  * (상세: ID switch 원인 분석 지점 정리:)
  * (상세: 멀고 겹치는 사람에서 ID switch가 남는 원인은 작은 bbox, 배경 섞임, occlusion, detection score 저하, NMS 손실 가능성이 크다고 분석함.)
  * (상세: Kalman filter 자체보다 detector 출력 품질, high/low score 분리, 1차/2차 association cost matrix를 먼저 봐야 한다고 정리함.)
  * (상세: BT_DEBUG_ASSOC, BT_DEBUG_LIMIT 환경변수 기반 association debug 로그를 추가해 iou, score_cost, appearance, final cost, 선택된 detection을 확인할 수 있게 함.)
* **[YOLOv7 detector 기반 ByteTrack 데모 구성](https://www.notion.so/yolov7-detector-기반-bytetrack-데모-구성-397c640ccca48023881afc70e4b5372c)**
  * (상세: 개요: ByteTrack 저장소에서 YOLOX 대신 YOLOv7 detector 결과를 넣어 추적하는 데모를 만들고, 외부 의존성 부담을 줄이기 위해 YOLOv7 로더를 저장소 내부에 vendoring함.)
  * (상세: 주요 진행 내용:)
  * (상세: ByteTrack 입력 포맷 확인:)
  * (상세: BYTETracker.update()가 5열 입력을 [x1, y1, x2, y2, score]로 처리하는 것을 확인함.)
  * (상세: YOLOv7 NMS 결과 [x1, y1, x2, y2, conf, cls]에서 사람 class만 남긴 뒤 [x1, y1, x2, y2, conf]로 변환해 tracker에 넘기는 구조로 정리함.)
  * (상세: YOLOv7 vendor 코드 추가:)
  * (상세: third_party/yolov7/ 아래에 YOLOv7 checkpoint 로딩과 forward에 필요한 models 구조를 추가함.)
  * (상세: 원본 YOLOv7 repo는 런타임 import하지 않고, 기본 weight 경로만 C:\Users\chlgu\Documents\GitHub\yolov7\yolov7.pt를 참조하도록 구성함.)
  * (상세: utils는 학습, 다운로드, 플롯 기능을 제외하고 데모에 필요한 letterbox, NMS, 좌표 복원, torch helper 중심으로 축소함.)
  * (상세: 데모 파이프라인 구현:)
  * (상세: tools/demo_yolov7_track.py를 추가해 cv2.VideoCapture -> YOLOv7 inference -> NMS -> Nx5 detection -> BYTETracker.update -> draw/write 흐름이 한 파일에서 보이도록 작성함.)
  * (상세: 기본 출력은 YOLOv7_ByteTrack_demo.mp4로 저장되도록 구성함.)
  * (상세: 샘플 비디오 한 프레임 기준 YOLOv7 detection과 ByteTrack update가 연결되는 smoke test를 수행함.)
  * (상세: 의존성 축소 및 호환성 정리:)
  * (상세: loguru, scipy, lap, cython_bbox가 없어도 데모 경로가 import될 수 있도록 tracker import와 matching 경로를 가볍게 정리함.)
  * (상세: lap이 있으면 LAPJV를 사용하고, 없으면 greedy matching fallback을 사용할 수 있게 구성함.)
  * (상세: Kalman filter의 SciPy 의존 경로는 NumPy 기반 연산으로 대체함.)
  * (상세: 최신 NumPy에서 문제가 되는 np.float 사용을 정리함.)
* **[로컬 추론 가속 환경 정리](https://www.notion.so/로컬-추론-가속-환경-정리-397c640ccca48040b204faf5f822e15d)**
  * (상세: 개요: Windows 11 로컬 장비에서 YOLO 추론을 어떤 가속 경로로 돌릴 수 있는지 NPU, CUDA, TensorRT 관점에서 확인함.)
  * (상세: 주요 진행 내용:)
  * (상세: Intel AI Boost NPU 사용 경로 확인:)
  * (상세: 장치 관리자에 Intel(R) AI Boost로 보이는 NPU는 OpenVINO 경유로 사용하는 방향이 현실적이라고 정리함.)
  * (상세: YOLO 모델은 OpenVINO 형식으로 export한 뒤 device="intel:npu"로 추론하는 흐름을 검토함.)
  * (상세: NPU는 저전력 추론용이라 실제 YOLO 영상 추론에서는 intel:npu, intel:gpu, intel:cpu를 모두 벤치마크해 비교해야 한다고 판단함.)
  * (상세: TensorRT engine과 CPU 실행 가능성 확인:)
  * (상세: TensorRT engine은 NVIDIA GPU 대상 런타임이므로 CPU에서 직접 실행하는 용도와 맞지 않는다고 정리함.)
  * (상세: Intel NPU/CPU/GPU는 OpenVINO, NVIDIA GPU는 TensorRT 경로로 분리해 보는 것이 맞다고 판단함.)
  * (상세: CUDA PyTorch 환경 문제 분리:)
  * (상세: nvidia-smi는 정상 출력되지만 torch.cuda.is_available()가 False로 나오는 문제를 확인함.)
  * (상세: 원인은 대개 CPU 전용 PyTorch wheel이 설치된 경우로 보고, torch.version.cuda 확인 후 CUDA 지원 wheel 재설치가 필요하다고 정리함.)
  * (상세: 드라이버의 CUDA Version: 13.0 표기는 드라이버 지원 범위이며, PyTorch wheel이 CUDA 13.0으로 설치되었다는 의미는 아니라고 구분함.)
  * (상세: OpenCV imshow 재생 속도 문제 확인:)
  * (상세: CUDA 활성화 후 imshow 영상이 너무 빠르게 지나가는 현상은 원본 FPS가 아니라 처리 FPS에 맞춰 화면을 갱신해서 발생한다고 정리함.)
  * (상세: 원본 비디오 FPS 기준으로 cv2.waitKey() delay를 계산하고, 추론 시간만큼 대기 시간을 보정하는 방식으로 해결 방향을 잡음.)

### ■ 07월 09일 (목요일)
* **[detection-only 재생 속도 보정](https://www.notion.so/detection-only-재생-속도-보정-398c640ccca4804daf17f27e899976c5)**
  * (상세: 개요: tracker를 끈 detection-only 모드에서도 원본 FPS 기준으로 영상을 확인할 수 있도록 재생 delay를 보정함.)
  * (상세: 주요 진행 내용:)
  * (상세: ea531af add delay on detection-only mode 커밋에서 detection-only 경로에 frame_interval 기반 waitKey delay 계산을 반영함.)
  * (상세: tracking 모드와 동일하게 처리 시간만큼 대기 시간을 보정해, detector 결과를 프레임별로 비교하기 쉽게 함.)
* **[색 히스토그램 feature 개선 및 시각화 패널 추가](https://www.notion.so/색-히스토그램-feature-개선-및-시각화-패널-추가-398c640ccca4802c99acd34cead759eb)**
  * (상세: 개요: bbox 전체 BGR 히스토그램은 배경색과 흰색/검은색 옷의 영향이 커서, 색 기반 association feature를 더 실험하기 쉽도록 weighted HSV 히스토그램과 실시간 표시 패널을 추가함.)
  * (상세: 주요 진행 내용:)
  * (상세: 히스토그램 패널 추가:)
  * (상세: -show-histograms 옵션을 추가해 재생 화면 오른쪽에 detection별 히스토그램 패널을 표시하도록 구현함.)
  * (상세: detection-only 화면과 tracking 화면 양쪽에서 같은 패널을 볼 수 있게 연결함.)
  * (상세: 원본 crop 기준으로 히스토그램을 계산해 bbox 선과 텍스트 색상이 feature에 섞이지 않도록 처리함.)
  * (상세: bin 단위 눈금 추가:)
  * (상세: 히스토그램 폴리라인만으로는 bin 위치를 구분하기 어려워, HISTOGRAM_BINS 개수만큼 세로 눈금을 추가함.)
  * (상세: 4번째 bin마다 조금 더 밝은 grid line을 표시해 값의 위치를 비교하기 쉽게 조정함.)
  * (상세: weighted HSV feature 적용:)
  * (상세: 기존 bbox 전체 BGR histogram 대신 HSV histogram을 사용하도록 appearance feature를 변경함.)
  * (상세: 가중치는 bbox 중앙 Gaussian * saturation^2 * value 형태로 적용해 외곽 배경, 흰색, 검은색 픽셀의 영향을 줄이고 채도 높은 원색 계열을 더 강하게 반영하도록 함.)
  * (상세: 색 정보가 거의 없는 crop은 feature를 만들지 않고 None으로 처리해 appearance cost가 불필요하게 흔들리지 않도록 함.)
  * (상세: 디버그 표시와 실제 feature 기준 동기화:)
  * (상세: 화면 패널도 실제 association feature와 같은 weighted HSV 방식으로 맞춤.)
  * (상세: 패널 제목을 Weighted HSV histograms로 변경하고, 유효한 색 정보가 부족한 박스는 low color로 표시하도록 정리함.)
* **[threshold 튜닝과 ID 유지 조건 분석](https://www.notion.so/threshold-튜닝과-id-유지-조건-분석-398c640ccca480249faff5c7de32cf9c)**
  * (상세: 개요: detector threshold, tracker threshold, match threshold가 ID switch와 누락에 미치는 영향을 로그와 detection-only 화면 기준으로 비교함.)
  * (상세: 주요 진행 내용:)
  * (상세: match_thresh 조정 영향 확인:)
  * (상세: match_thresh를 타이트하게 잡으면 멀리 있는 사람의 잘못된 점프는 줄어드는 반면, 가까운 큰 사람 1, 2가 겹치는 순간 기존 track이 lost 처리되거나 ID가 바뀌기 쉬운 것을 확인함.)
  * (상세: 전체 threshold를 더 조이는 것보다 rank>1이면서 iou가 낮거나 jump가 큰 매칭만 별도로 reject하는 방식이 더 적절하다고 판단함.)
  * (상세: detector confidence와 tracker threshold 역할 분리:)
  * (상세: -conf-thres 0.3으로 낮추면 detection-only 화면에서는 대부분의 사람이 잡히지만, association 이후에는 confidence가 높은 객체 위주로 추적되는 현상을 확인함.)
  * (상세: ByteTrack 내부에서 track_thresh 이상 detection은 1차 association과 신규 track 후보가 되고, 그보다 낮은 detection은 2차 association 유지용으로만 쓰이는 구조를 정리함.)
  * (상세: track_thresh를 너무 낮추면 low-confidence box가 1차 association에 들어와 겹치지 않는 상황에서도 track이 튀는 부작용이 있음을 확인함.)
  * (상세: 후속 reject 조건 후보 정리:)
  * (상세: rank > 1이고 iou < 0.4 또는 jump가 큰 경우에는 다른 사람에게 ID가 넘어가는 것보다 해당 track을 lost로 남기는 쪽이 안정적일 수 있다고 판단함.)
  * (상세: 다만 큰 사람의 occlusion 구간에서는 너무 강한 reject가 track 단절을 늘릴 수 있어, 현재 가장 안정적인 association 파라미터를 기준으로 보조 gate를 추가하는 방향을 후속 검토 항목으로 남김.)
* **[ID switch 분석을 위한 association 로그 개선](https://www.notion.so/id-switch-분석을-위한-association-로그-개선-398c640ccca4807696acee2ae8a18ce9)**
  * (상세: 개요: YOLOv7 + ByteTrack 실험 중 ID가 다른 사람에게 넘어가는 순간을 로그만으로 구분하기 어려워, -debug-association 출력에 매칭 품질과 후보 순위를 함께 표시하도록 개선함.)
  * (상세: 주요 진행 내용:)
  * (상세: association 선택 결과 가시화:)
  * (상세: 기존 로그는 track_id와 detection index 위주로 출력되어, 같은 ID가 다른 사람에게 붙었는지 판단하기 어려웠음.)
  * (상세: 선택된 detection에 대해 rank, margin, 실제 iou, jump, 예측 중심점 pred, detection 중심점 det를 함께 출력하도록 정리함.)
  * (상세: iou가 낮거나 중심점 이동량이 큰 경우 SUSPECT_SWITCH 플래그를 붙여 의심 프레임을 빠르게 찾을 수 있게 함.)
  * (상세: 후보별 cost 해석 정리:)
  * (상세: final_dists는 유사도가 아니라 cost matrix이므로 값이 낮을수록 좋은 후보라고 정리함.)
  * (상세: np.argsort(final_dists[row])[:2]는 각 track 기준 cost가 가장 낮은 후보 1, 2순위를 가져오는 동작임을 확인함.)
  * (상세: 후보 출력에서 실제 IoU와 혼동되지 않도록 cost 값은 iou_cost, score_cost, app, final 형태로 구분함.)
  * (상세: rank 의미와 ID switch 케이스 분석:)
  * (상세: rank=1은 해당 track 기준 최선 후보가 선택된 상태이고, rank>1은 전체 assignment 결과 때문에 차선 후보가 선택된 상태로 정리함.)
  * (상세: 실제 switch 로그에서 track#019가 자기 기준 1순위 det#00 대신 2순위 det#05에 붙는 사례를 확인함.)
  * (상세: 해당 상황은 det#00을 이미 track#001이 가져가 전체 비용 합이 더 낮아졌기 때문에 발생한 것으로 분석함.)
  * (상세: LAP/Hungarian assignment 동작 확인:)
  * (상세: matching.linear_assignment() 내부의 lap.lapjv(cost_matrix, extend_cost=True, cost_limit=thresh)가 track-detection 1:1 전체 조합의 cost 합을 최소화하는 구조임을 정리함.)
  * (상세: x[i]는 i번 track이 선택한 detection index, y[j]는 j번 detection에 배정된 track index이며, 1은 unmatched 상태로 해석함.)
  * (상세: match_thresh는 전체 매칭 이후 cost limit으로 동작하므로, 단일 track의 최선 후보만 보는 방식과 결과가 다를 수 있음을 확인함.)

### ■ 07월 10일 (금요일)
* **[tracker 하위 type-safe 리팩터링](https://www.notion.so/tracker-하위-type-safe-리팩터링-399c640ccca48084ab6be1d85d410c7c)**
  * (상세: 개요: tracker 디렉터리 코드의 타입 불명확성과 상태 접근 문제를 줄이고, 기존 추적 동작을 유지하면서 정적 타입 검사에 대응할 수 있도록 리팩터링함.)
  * (상세: 주요 진행 내용:)
  * (상세: basetrack.py, byte_tracker.py, kalman_filter.py, matching.py, association.py, association_report.py의 함수 인자와 반환 타입을 명시함.)
  * (상세: NumPy 배열에 FloatArray, IntArray 타입을 적용해 cost matrix, bbox, detection score, 매칭 결과의 자료형을 구분함.)
  * (상세: Kalman filter와 track 상태의 None 가능성을 명시적으로 처리해 초기화되지 않은 필터 접근을 방지함.)
  * (상세: association observer와 tracker 설정의 데이터 흐름을 타입 기준으로 정리하고, pyrightconfig.json을 추가해 tracker 범위의 기본 타입 검사를 구성함.)
  * (상세: dataclass와 slots 등을 적용해 snapshot/decision 데이터의 구조와 불변성을 명확히 함.)
* **[ID switch 분석 리포트 및 Lost-Tracked 충돌 판별 보강](https://www.notion.so/id-switch-분석-리포트-및-lost-tracked-충돌-판별-보강-399c640ccca480029566d1fda3c26603)**
  * (상세: 개요: YOLOv7 + ByteTrack 실험에서 ID switch가 발생하는 association 상황을 비용 행렬과 전후 프레임 흐름으로 분석할 수 있도록 진단 리포트를 구성하고, 기존 로직으로 놓치던 Lost 트랙의 잘못된 재활성화 사례를 별도 판별하도록 보강함.)
  * (상세: 주요 진행 내용:)
  * (상세: association 분석 구조 분리:)
  * (상세: tracker/association.py에 프레임별 트랙·detection 박스, IoU/score/appearance/final cost, 매칭 결과를 담는 snapshot과 분석 결과 타입을 정리함.)
  * (상세: tracker/association_report.py에 이벤트 버퍼와 리포트 writer를 분리해, tracker가 시각화 코드에 직접 의존하지 않도록 구성함.)
  * (상세: 한 이벤트를 오버레이, IoU·Appearance·Final cost heatmap, 전후 rank/cost timeline으로 저장하는 흐름을 구성함.)
  * (상세: Lost 트랙의 Tracked 트랙 대체 상황 판별 추가:)
  * (상세: Lost 상태 트랙이 detection을 가져간 시점에, 매칭되지 않은 Tracked 트랙이 해당 detection을 유일한 유효 후보로 보고 있었는지 분석하도록 추가함.)
  * (상세: lost_displaced_tracked 사유와 밀려난 track ID를 로그 및 리포트 제목에 표시하도록 정리함.)
  * (상세: 리포트 오버레이에는 밀려난 Tracked prediction을 분홍색 점쇄선으로 표시하고, heatmap에는 해당 detection 후보 칸을 별도로 표시하도록 보강함.)
  * (상세: 실제 association 결과를 변경한 것은 아니며, 현재는 문제 상황을 관찰·분류하는 단계로 유지함.)
  * (상세: frame 272의 T4/T19 사례 확인:)
  * (상세: Lost 상태였던 T4가 D3으로 재활성화되는 동안, 정상 추적 중이던 T19가 동일 detection을 놓치는 전환 패턴을 합성 snapshot으로 재현함.)
  * (상세: T4의 appearance cost가 T19보다 불리했음에도 IoU·score 결합 결과가 근소하게 앞서면서 Lost 트랙이 detection을 가져가는 상황을 리포트 대상으로 확인함.)
  * (상세: 색 히스토그램 자체의 개선 효과는 유지하되, 이번 사례의 우선 원인은 Tracked와 Lost 트랙을 동일 association pool에서 경쟁시키는 상태 전환 정책 부재로 정리함.)
  * (상세: 리포트 생성 및 분석 기능 정리:)
  * (상세: -debug-association, verbose logger를 통해 frame/track/cost/rank/margin/후보 소유자 정보를 확인할 수 있도록 로그 형식을 보강함.)
  * (상세: 생성된 리포트의 판독 순서와 heatmap 범례를 report/README.md에 문서화함.)
  * (상세: 실험 산출물인 report/*, 영상 파일은 Git 추적 대상에서 제외하도록 정리함.)

---

## 📝 2. 계획 대비 완료/미완료 대조 (7월 2주차 대조 표)

| 7월 2주차 계획 | 완료여부 | 7월 2주차 Daily Work Log |
| :--- | :---: | :--- |
---

## 🚀 3. 이번 주 업무 계획 (7월 3주차)

### 🔗 Task: [시스템 구현](https://www.notion.so/시스템-구현-368c640ccca480d2a400d2cca052ed7d) (~7/31)
* 객체 추적 알고리즘 고도화

### 🔗 Task: [[시스템 리빌딩]웹 페이지 UI](https://www.notion.so/시스템-리빌딩웹-페이지-ui-368c640ccca480a99e43ffdc5542f3e2) (~6/5)
* 기기 제어 버튼 변경

