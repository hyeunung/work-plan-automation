# 📅 [7월 3주차] 주간 업무 브리핑
> 월요일 출근 시간 전, 지난주 업무 분석 및 금주 계획 종합 브리핑입니다.

---

## 📂 1. 지난주 실제 업무 보고 (Daily Work Log) (7/13~7/19)

### ■ 07월 13일 (월요일)
* **[Point cloud 및 3D bbox 기반 추적 방향 브레인스토밍](https://www.notion.so/point-cloud-및-3d-bbox-기반-추적-방향-브레인스토밍-39cc640ccca48049acb8c5f5e8eebfcf)**
  * (상세: 개요: STrack.tlwh에 단일 depth 값을 추가하는 방식에서 확장해, bbox 내부의 모든 유효 depth point를 3D 관측 표면으로 사용하고 3D detection·tracking으로 전환하는 방향을 검토함.)
  * (상세: 주요 진행 내용:)
  * (상세: point set 기반 geometry 개념 정리:)
  * (상세: bbox 내부 depth pixel을 deprojection한 (X, Y, Z) point set을 track geometry의 일부로 보는 방향을 논의함.)
  * (상세: 이 표현은 객체의 실제 부피보다 카메라에서 관측된 표면을 나타내므로, 일반적인 3D volume IoU보다 surface overlap, point-support overlap, visibility consistency에 가깝다고 정리함.)
  * (상세: point cloud는 프레임마다 표본 위치가 달라 동일 점의 단순 집합 교집합을 계산하기 어렵고, voxel 단위 허용 범위나 근접 거리 기준이 필요함을 확인함.)
  * (상세: 동일한 현재 depth frame에서 track bbox와 detection bbox를 각각 잘라 비교하면 기존 2D IoU에 depth valid mask를 적용한 것과 유사해져, depth의 시간적 정보가 충분히 활용되지 않을 수 있다고 판단함.)
  * (상세: 시간축 point cloud 비교의 쟁점 확인:)
  * (상세: 이전 track point set과 현재 detection point set을 비교해야 새로운 depth 정보가 생기지만, 객체 이동과 카메라 이동으로 절대 좌표 overlap이 쉽게 감소하는 문제를 확인함.)
  * (상세: 카메라 절대좌표 비교, 중심 정렬 후 형상 비교, 예측 위치로 이동한 surface 비교가 서로 다른 의미를 가진다고 정리함.)
  * (상세: 가림 상황에서는 대칭 IoU보다 현재 detection point가 기존 track surface로 설명되는지를 보는 방향성 coverage나 visibility consistency가 더 적합할 수 있다고 판단함.)
  * (상세: 3D bbox detector 연계 검토:)
  * (상세: stereo point cloud에서 3D bbox를 직접 출력하는 detector를 사용하면 2D IoU에 depth를 혼합하는 문제 대신 3D center distance, BEV IoU, 3D IoU 기반 association으로 전환할 수 있다고 정리함.)
  * (상세: ByteTrack의 high/low-confidence 2단계 association 개념은 유지하면서 geometry와 Kalman 상태만 3D 공간으로 확장할 수 있음을 확인함.)
  * (상세: full-scene 3D detector보다 기존 2D YOLO person bbox를 proposal로 사용해 bbox 내부 point cloud에서 foreground를 분리하고 3D bbox를 추정하는 frustum 방식이 현재 구조와 더 자연스럽게 연결된다고 판단함.)
  * (상세: 이 방향의 핵심 과제는 bbox 내부의 사람 전경·배경 분리, 보이는 표면만으로 안정적인 3D bbox를 정의하는 방법, RealSense 환경에 맞는 사람 3D bbox 데이터와 detector 품질로 정리함.)
* **[IoU와 depth를 결합한 geometry association 설계 검토](https://www.notion.so/iou와-depth를-결합한-geometry-association-설계-검토-39cc640ccca480138243dd747764b6fd)**
  * (상세: 개요: RealSense/stereo depth를 ByteTrack의 보조 appearance feature가 아닌 공간 기하 정보로 활용하기 위해, 모든 association stage에서 IoU와 depth를 함께 사용하는 방향을 검토함.)
  * (상세: 주요 진행 내용:)
  * (상세: second association의 depth 적용 근거 정리:)
  * (상세: low-confidence detection은 score를 다시 반영하면 이중 감점될 수 있고, appearance도 가림·블러·부정확한 bbox에서 오염될 가능성이 있어 기존 second 단계에서는 제외된 것으로 정리함.)
  * (상세: stereo depth는 detector confidence와 별개로 유효할 수 있는 기하 관측이므로, 유효한 depth가 있는 경우 second association에도 적용할 가치가 있다고 판단함.)
  * (상세: depth가 invalid하거나 신뢰도가 낮으면 기존 IoU cost로 fallback하는 조건을 기본 원칙으로 정함.)
  * (상세: depth 관측 신뢰도 논의:)
  * (상세: bbox 중심 한 점이 아니라 내부 ROI의 유효 depth 중앙값을 대표 거리로 사용하는 방향을 검토함.)
  * (상세: valid pixel ratio, MAD 또는 분산, stereo confidence를 이용해 depth 관측 신뢰도를 계산하는 방안을 정리함.)
  * (상세: raw meter 차이를 cost에 직접 더하지 않고 거리에 따라 증가하는 오차 허용 범위로 정규화해야 한다고 판단함.)
  * (상세: 양쪽 depth가 모두 고신뢰이고 거리 차이가 큰 경우에만 hard gate를 적용하고, 초기에는 부드러운 mismatch penalty로 시작하는 방향을 검토함.)
  * (상세: geometry base cost 구조 제안:)
  * (상세: RealSense 전용 입력이라면 depth를 선택적 후처리 layer로 두기보다 IoU와 depth를 결합한 geometry_distance를 모든 stage의 base cost로 사용하는 구조가 더 자연스럽다고 판단함.)
  * (상세: 권장 흐름을 first: geometry → score → appearance, second: geometry, unconfirmed: geometry → score → appearance로 정리함.)
  * (상세: AssociationStage에 base_cost callable을 추가하고, AssociationPipeline.run()이 고정된 IoU 호출 대신 stage별 base cost를 실행하는 확장안을 검토함.)
  * (상세: 디버깅 시 depth가 최종 매칭에 미친 영향을 분리할 수 있도록 iou_cost, depth_cost, depth_confidence, geometry_cost, final_cost를 각각 보존할 필요가 있다고 정리함.)
  * (상세: 공간 분할 depth 표현 검토:)
  * (상세: IoU 자체를 max pooling하는 방식은 작은 겹침 영역을 과대평가할 수 있어 적합하지 않다고 판단함.)
  * (상세: bbox 내부를 고정 격자로 나누고 각 구역의 depth median과 신뢰도를 계산하는 spatial depth descriptor 방향을 논의함.)
  * (상세: IoU는 화면상 위치 제약으로 유지하고, spatial depth는 후보 간 거리 불일치를 판별하는 역할로 분리하는 것이 해석에 유리하다고 정리함.)
* **[ByteTrack association 파이프라인 리팩터링](https://www.notion.so/bytetrack-association-파이프라인-리팩터링-39cc640ccca480b19a26e14d50f94942)**
  * (상세: 개요: BYTETracker.update()의 first, second, unconfirmed association 단계에서 반복되던 cost 계산과 assignment 흐름을 공통 파이프라인으로 분리하고, 단계별 정책은 설정으로 구분하도록 리팩터링함.)
  * (상세: 주요 진행 내용:)
  * (상세: association 공통 구조 추출:)
  * (상세: tracker/matching.py에 CostLayer, AssociationStage, AssociationResult, AssociationPipeline을 추가함.)
  * (상세: 각 단계에서 반복하던 기본 IoU cost 계산, score·appearance cost 적용, linear assignment, unmatched 결과 정리를 pipeline.run() 호출로 통합함.)
  * (상세: 단계별 cost와 매칭 결과를 AssociationResult에 보존해 이후 관찰 및 디버깅 기능과 연결할 수 있도록 구성함.)
  * (상세: 단계별 association 정책 유지:)
  * (상세: first association은 high-confidence detection을 대상으로 score와 appearance layer를 적용하도록 구성함.)
  * (상세: second association은 ByteTrack의 low-confidence 복구 의미를 유지하기 위해 기본 IoU-only 정책으로 둠.)
  * (상세: unconfirmed association에는 score와 appearance layer를 적용하고 별도 threshold를 유지함.)
  * (상세: detection의 high/low 분리, Kalman prediction, update, re_activate, mark_lost, mark_removed 등 트랙 상태 전이는 기존 BYTETracker.update()에 남겨 association 계산과 lifecycle 책임을 분리함.)
  * (상세: 확장 범위 검토:)
  * (상세: 실행 중 레이어를 변경하는 mutable API나 범용 플러그인 레지스트리는 추가하지 않고, 생성 시 고정된 layer tuple을 전달하는 작은 구조로 제한함.)
  * (상세: plugin registry를 도입하면 레이어를 이름과 설정으로 동적 조립할 수 있지만, 이름 충돌, 순서, 설정 검증, 전역 상태 오염까지 관리해야 함을 정리함.)
  * (상세: 상태 전이까지 단계 객체로 이동할 경우 first·second·unconfirmed·신규 활성화·만료 정책을 각각 lifecycle 객체로 분리해야 하며, 전체 구조가 tracker workflow engine으로 확대된다고 판단함.)

### ■ 07월 14일 (화요일)
* **[DeviceCard 전원 토글 클릭 영역 개선](https://www.notion.so/devicecard-전원-토글-클릭-영역-개선-39dc640ccca4800db8d2c44b65ee90ad)**
  * (상세: 개요: DevicePage에서 DeviceCard의 어느 영역을 클릭해도 장치 활성화 상태가 변경되던 동작을 수정해, 좌측 상단 번개 아이콘 영역을 클릭했을 때만 전원 토글이 실행되도록 개선함.)
  * (상세: 주요 진행 내용:)
  * (상세: 카드 전체 클릭 토글 제거:)
  * (상세: DeviceCard.tsx의 Card 루트에 연결된 onClick 핸들러를 제거해 카드 본문, 장치명, 상태 정보 등 비제어 영역을 클릭해도 onToggle이 호출되지 않도록 수정함.)
  * (상세: 카드 전체에 적용되던 cursor-pointer 스타일을 제거해 실제 조작 가능 영역과 시각적 동작 범위를 일치시킴.)
  * (상세: 번개 아이콘 영역을 전원 버튼으로 변경:)
  * (상세: 좌측 상단의 40×40 상태 아이콘 컨테이너를 button 요소로 변경하고, 해당 버튼에만 onToggle(actuator, !isOn)을 연결함.)
  * (상세: 토글 처리 중에는 버튼을 disabled 상태로 두고 로딩 아이콘 및 cursor-not-allowed 스타일을 표시해 중복 입력을 방지함.)
  * (상세: 현재 전원 상태에 따라 전원 켜기 또는 전원 끄기 의미가 전달되도록 장치명을 포함한 aria-label을 추가함.)
  * (상세: 단위 테스트 기준 변경:)
  * (상세: 기존 카드 루트 클릭 테스트를 카드 전체에는 onClick이 없고 번개 아이콘 버튼에만 클릭 핸들러가 존재하는지 확인하는 테스트로 변경함.)
  * (상세: 번개 아이콘 버튼 클릭 시 대상 장치와 반전된 활성화 상태가 onToggle에 전달되는지 검증함.)
  * (상세: 커밋 및 검증 결과:)
  * (상세: 372d484 refactor: Device Card 토글 조건 변경 커밋으로 DeviceCard.tsx와 DeviceCard.test.tsx 변경을 반영함.)
  * (상세: DeviceCard 단위 테스트 4개 통과 및 프로덕션 빌드 성공을 확인함.)
* **[D555 point cloud 토픽 미생성 원인 진단](https://www.notion.so/d555-point-cloud-토픽-미생성-원인-진단-39dc640ccca480879ae0d3ac966558da)**
  * (상세: 개요: Jetson에서 rs_launch.py에 color, depth, pointcloud, align_depth 활성화 인자를 전달했음에도 PointCloud2 토픽이 생성되지 않는 문제를 실행 로그와 파라미터 상태를 기준으로 진단함.)
  * (상세: 주요 진행 내용:)
  * (상세: 실행 상태 및 증상 분리:)
  * (상세: RealSense ROS 4.58.2, librealsense 2.58.2, D555 장치와 color/depth 896x504x30 스트림이 정상적으로 시작되는 것을 확인함.)
  * (상세: 실행 노드에서 enable_depth=True, enable_color=True가 적용됐지만 pointcloud.enable 조회 결과는 Parameter not set으로 나타남.)
  * (상세: depth/color 스트림이나 RViz2 표시 문제가 아니라 launch 인자와 실제 노드의 pointcloud 필터 파라미터 이름이 일치하지 않는 문제로 범위를 좁힘.)
  * (상세: Jetson NEON 파라미터 명명 불일치 확인:)
  * (상세: Jetson ARM 환경에서 librealsense pointcloud 필터가 Pointcloud (NEON) 이름으로 생성되고, 현재 wrapper가 이를 표준 Pointcloud 이름으로 정규화하지 못하는 문제를 확인함.)
  * (상세: 실제 노드에는 pointcloud.enable 대신 pointcloud__neon_.enable, pointcloud__neon_.stream_filter, pointcloud__neon_.ordered_pc 등 NEON 접미사가 포함된 파라미터가 선언됨.)
  * (상세: pointcloud.enable:=true launch 인자는 존재하지 않는 표준 이름에 전달되고, 실제 pointcloud__neon_.enable은 False로 남아 필터와 publisher가 생성되지 않았다고 진단함.)
  * (상세: 런타임 우회 검증:)
  * (상세: 실행 중인 노드에 ros2 param set /camera/camera 'pointcloud__neon_.enable' true를 적용해 실제 NEON pointcloud 필터를 활성화함.)
  * (상세: 설정 직후 /camera/camera/depth/color/points 토픽이 생성되는 것을 확인해 D555 장치, depth/color profile, align_depth, RViz2가 원인이 아님을 검증함.)
  * (상세: 런타임 파라미터 변경은 노드 재시작 시 초기화되는 임시 조치이며, 영구 해결을 위해 wrapper의 필터 이름 변환표에 Pointcloud (NEON) -> Pointcloud 매핑을 추가하고 재빌드해야 한다고 정리함.)
* **[AGV 사용자 추종을 위한 3D 위치 Kalman 설계 검토](https://www.notion.so/agv-사용자-추종을-위한-3d-위치-kalman-설계-검토-39dc640ccca480779078e4d9da007aa5)**
  * (상세: 개요: 사용자를 일정 거리 이내에서 따라가는 AGV 환경에서는 사용자와 카메라가 함께 이동해 depth가 상시 변하므로, 마지막 거리와 현재 관측을 비교하는 IoU + depth 감점 방식에서 상대 위치·속도를 예측하는 Kalman 기반 3D 추적으로 확장하는 방향을 검토함.)
  * (상세: 주요 진행 내용:)
  * (상세: 기존 depth penalty 방식의 한계 정리:)
  * (상세: 동일 사용자도 AGV 및 사용자 이동에 따라 프레임별 depth가 크게 달라질 수 있어, 저장된 단일 depth와 현재 detection의 거리 차이만 사용하면 정상 매칭을 불일치로 판단할 수 있음을 확인함.)
  * (상세: 단순히 같은 거리를 유지하는지 비교하기보다, 이전 위치와 상대 속도로 예측한 현재 위치가 detection 관측과 일치하는지를 판단해야 한다고 정리함.)
  * (상세: AGV 목적에 맞는 상태 정의:)
  * (상세: 사람의 3D bbox 크기·yaw까지 추정하는 상태보다 로봇 기준 사용자의 상대 위치와 속도를 나타내는 (X, Z, Vx, Vz) 상태가 추종 제어에 직접적이라고 판단함.)
  * (상세: X는 좌우 조향 오차, Z는 전방 추종 거리, Vx와 Vz는 횡방향 및 전후방 상대 이동 예측에 사용하는 구조로 정리함.)
  * (상세: 경사로나 높이 변화가 중요한 환경이 확인될 때만 (X, Y, Z, Vx, Vy, Vz)로 확장하고, 현재 평면 주행 AGV에는 지면 평면상의 위치 추적부터 적용하는 방향을 제안함.)
  * (상세: RealSense 3D 관측 생성 방향 검토:)
  * (상세: color 좌표계로 정렬한 depth와 카메라 intrinsics를 사용해 detection의 (u, v, Z)를 (X, Y, Z)로 deprojection하는 흐름을 정리함.)
  * (상세: bbox 중심 한 점보다 몸통 영역 또는 bbox 내부 point cloud의 robust 중심을 사용해 배경, invalid depth, 팔·다리 자세의 영향을 줄이는 방안을 검토함.)
  * (상세: depth valid ratio, MAD, bbox 품질을 detection별 측정 covariance에 반영하고, depth가 유효하지 않으면 2D IoU association으로 fallback하는 원칙을 유지함.)
  * (상세: 카메라 ego-motion 처리 필요성 확인:)
  * (상세: 이동형 AGV에서는 카메라 좌표의 사용자 움직임에 AGV 자체 이동과 회전이 포함되므로, 보정하지 않으면 Kalman filter가 로봇의 움직임을 사용자의 속도로 오인할 수 있음을 확인함.)
  * (상세: 로봇 상대 좌표계를 유지할 경우 odometry/IMU로 이전 track 상태를 현재 로봇 좌표로 변환하고, odom/world 좌표계를 사용할 경우 카메라 좌표를 base 및 odom 좌표로 변환한 뒤 추적하는 두 방안을 정리함.)
  * (상세: 신뢰 가능한 odometry가 있다면 world/odom 좌표에서 사용자를 추적하고 제어 시 로봇 상대 좌표로 변환하는 구조가 장시간 lost track 복구에도 유리하다고 판단함.)
  * (상세: 3D association 및 추종 안전 정책 정리:)
  * (상세: track의 예측 (X, Z)와 detection 관측 간 Mahalanobis distance로 3D gate를 구성하고, 통과한 후보에 2D IoU와 appearance cost를 적용하는 흐름을 제안함.)
  * (상세: Kalman covariance를 이용하면 최근 관측된 track은 좁은 후보 범위, 오래 lost된 track은 넓은 후보 범위를 갖게 되어 stale depth의 불확실성을 자연스럽게 반영할 수 있음을 정리함.)
  * (상세: 다중 객체의 track ID와 실제 추종 대상 사용자를 분리하고, 대상이 불확실하거나 장시간 lost된 경우 다른 사람에게 자동 전환하지 않고 감속·정지 또는 재탐색 상태로 전환해야 한다고 정리함.)

### ■ 07월 15일 (수요일)
* **[컨테이너 pip 및 librealsense2 잔류 경로 진단](https://www.notion.so/컨테이너-pip-및-librealsense2-잔류-경로-진단-39ec640ccca480f6b1f4ff6e3e04462b)**
  * (상세: 개요: ROS 2 Humble 컨테이너에서 Python은 있지만 pip 명령이 없는 이유와, agv_bringup 단독 빌드 시 존재하지 않는 install/librealsense2를 요구하는 원인을 확인함.)
  * (상세: 주요 진행 내용:)
  * (상세: Python/pip 패키지 구성 확인:)
  * (상세: Ubuntu에서는 python3와 python3-pip가 별도 패키지이며 현재 Dockerfile에 python3-pip, python3-venv가 설치되지 않은 상태임을 확인함.)
  * (상세: Windows에서 생성된 루트 .venv는 Linux 컨테이너에서 사용할 수 없고, ROS 2 Humble의 시스템 Python 3.10 환경과 루트 프로젝트의 Python 3.11 이상 환경을 분리할 필요가 있다고 정리함.)
  * (상세: colcon의 stale dependency 경로 확인:)
  * (상세: agv_bringup -> realsense2_camera -> librealsense2 의존 관계에 따라 과거 빌드에서 생성된 build/agv_bringup/colcon_command_prefix_build.sh가 삭제된 install/librealsense2/.../package.sh를 source하고 있음을 확인함.)
  * (상세: 워크스페이스 루트의 librealsense/가 colcon 패키지로 검색된 흔적이 남은 상태로 판단하고, 해당 경로에 COLCON_IGNORE를 두거나 -base-paths src로 검색 범위를 제한하는 방법을 정리함.)
  * (상세: stale build/agv_bringup 또는 관련 패키지 build 캐시를 제거한 뒤 -packages-up-to agv_bringup으로 의존 패키지를 재구성하는 절차를 제안함.)
* **[속도 명령 중재 흐름과 잔류 safety_gate 진단](https://www.notion.so/속도-명령-중재-흐름과-잔류-safetygate-진단-39ec640ccca48049bef3c04e9397bf32)**
  * (상세: 개요: twist_mux의 역할과 /cmd_vel_out 토픽 생성 원인을 코드·launch 기준으로 확인하고, m2_bench.launch.py 종료 후 safety_gate 노드가 중복·잔류하는 현상을 진단함.)
  * (상세: 주요 진행 내용:)
  * (상세: 속도 명령 중재 경로 확인:)
  * (상세: /cmd_vel_teleop, /cmd_vel_follow, /cmd_vel_row를 각각 우선순위 100, 50, 40으로 수신하고 0.5초 timeout 기준으로 하나의 속도 명령을 선택하는 구조를 확인함.)
  * (상세: estop과 bumper lock은 우선순위 255, 254로 일반 속도 명령을 차단하도록 설정돼 있음을 확인함.)
  * (상세: twist_mux의 기본 출력 publisher 이름은 cmd_vel_out이며, 현재 m2_bench.launch.py에서는 이를 /cmd_vel_mux로 remap해 safety_gate에 전달하도록 구성돼 있음을 확인함.)
  * (상세: 중복 safety_gate 원인 범위 축소:)
  * (상세: 현재 launch 포함 관계에서는 safety_gate가 한 번만 실행되므로, 두 번째 노드는 이전 ros2 run 또는 launch에서 남은 프로세스일 가능성이 높다고 판단함.)
  * (상세: pgrep -af로 실제 프로세스를 식별하고 TERM 신호로 정상 종료한 뒤, 필요한 경우에만 KILL을 사용하는 절차를 정리함.)
  * (상세: 프로세스 종료 후에도 노드 목록에 남는 경우 ros2 daemon 재시작은 ROS 그래프 표시 캐시 갱신용이며 실제 노드 종료 명령은 아님을 구분함.)
* **[Python 패키지 설치 및 follow_controller 파라미터 적용 문제 수정](https://www.notion.so/python-패키지-설치-및-followcontroller-파라미터-적용-문제-수정-39ec640ccca480cbaef7f01d5e277d7a)**
  * (상세: 개요: agv_perception 빌드는 성공하지만 launch 시 Python 모듈을 찾지 못하는 문제와, follow_controller.yaml을 추가했는데도 실행 파라미터가 기본값 0.0으로 유지되는 문제를 install-space 기준으로 진단함.)
  * (상세: 주요 진행 내용:)
  * (상세: ModuleNotFoundError 원인 교정:)
  * (상세: 소스 파일을 src/ 바로 아래로 이동한 뒤 기존 package 탐색 기준이 해당 파일을 Python 패키지로 설치하지 않아 실행 스크립트만 생성된 상태임을 확인함.)
  * (상세: setup.py에 패키지 목록과 package_dir 매핑을 명시해 agv_perception.yolo_detector:main entry point가 install-space 모듈을 정상 참조하도록 수정함.)
  * (상세: controller YAML 전달 경로 정리:)
  * (상세: follow_controller.yaml의 실제 노드 키를 follow_controller로 맞추고 target distance, 선형·각속도 gain 및 최대 속도를 설정함.)
  * (상세: controller.launch.py에서 package share의 YAML을 찾아 /follow_controller 노드에 parameters로 전달하도록 구성함.)
  * (상세: CMakeLists.txt의 설치 대상에 config/를 추가하고, Python 노드의 declare_parameter() 선언을 활성화해 YAML override가 적용될 수 있게 함.)
  * (상세: 제어 파라미터 역할 확인:)
  * (상세: target_distance_m은 목표 추종 거리, linear_gain은 거리 오차의 선속도 변환 비율, angular_gain은 좌우 편차의 회전 속도 변환 비율로 정리함.)
  * (상세: max_linear_mps, max_angular_radps는 출력 절댓값 상한이며 현재 YAML의 max_angular_radps=0.0에서는 회전 출력이 항상 0임을 확인함.)
* **[YOLOv7 + ByteTrack ROS 2 perception 패키지 이관](https://www.notion.so/yolov7-bytetrack-ros-2-perception-패키지-이관-39ec640ccca480dcaeb7d1bdc78c50cd)**
  * (상세: 개요: 로컬 realsense-track의 사람 검출·추적 코드를 ROS 2 agv_perception 패키지로 이관하고, D555 컬러 이미지 토픽을 구독해 track ID가 포함된 detection 메시지를 발행하도록 구성함.)
  * (상세: 주요 진행 내용:)
  * (상세: ROS 2 노드 및 입출력 구성:)
  * (상세: /camera/color/image_raw의 sensor_msgs/Image를 구독해 YOLOv7 사람 검출과 ByteTrack 추적을 수행하는 yolo_ros 실행 노드를 추가함.)
  * (상세: 결과를 /yolo/tracking의 vision_msgs/Detection2DArray로 발행하고 Detection2D.id에 ByteTrack track ID를 기록하도록 함.)
  * (상세: 직접 RealSense USB 장치를 여는 pyrealsense2 경로를 제거하고 ROS 이미지 토픽을 입력 경계로 사용하도록 변경함.)
  * (상세: 런타임 코드 정리:)
  * (상세: ByteTrack 핵심 추적 코드는 tracker.py, 실제 사용하는 YOLO 전처리·NMS·좌표 복원 헬퍼는 yolo_utils.py로 통합함.)
  * (상세: 모델 체크포인트 호환에 필요한 YOLOv7 모델 클래스는 src/models에 유지함.)
  * (상세: OpenCV 창, 영상 저장, 그래프, association report 등 시각화·분석용 실행 경로는 이관 대상에서 제외함.)
  * (상세: 설정 및 weight 관리 구조 추가:)
  * (상세: 모델 경로, 입력·출력 토픽, 추론 주기, confidence/IoU threshold, ByteTrack 및 appearance 관련 값을 config/yolo_tracker.yaml에서 관리하도록 함.)
  * (상세: 모델 파일은 패키지의 weight/ 디렉터리에서 관리하고 install-space의 package share 경로로 해석하도록 구성함.)
  * (상세: 실제 weight 바이너리는 원본 저장소에 없어 복사하지 않고 weight/yolov7.pt 배치 방법을 README로 문서화함.)
  * (상세: 패키지 설치 구조 구성:)
  * (상세: ament_python, console_scripts, launch/config/weight data file 설치 구성을 추가함.)
  * (상세: 중복 패키지 디렉터리를 만들지 않고 src/를 agv_perception, src/models/를 agv_perception.models로 매핑하는 setuptools package_dir 구성을 적용함.)
  * (상세: a4cfe1d feat: yolo tracker 추가 및 agv_controller 파라미터 파일로 관리하도록 변경 커밋으로 perception 패키지와 controller 설정 변경을 반영함.)
* **[Specific 시간 조건의 else action 등록 차단](https://www.notion.so/specific-시간-조건의-else-action-등록-차단-39ec640ccca480fdaf0ce83e2506fa6f)**
  * (상세: 개요: specific 시간 조건은 특정 시각에 한 번 실행되는 조건이어서 조건 탈출 시 실행되는 "어떻게 종료할까요?" 동작으로 분기할 수 없으므로, 저장 전에 잘못된 조합을 검증하도록 개선함.)
  * (상세: 주요 진행 내용:)
  * (상세: 저장 전 조건 조합 검증 추가:)
  * (상세: 자동화 저장 흐름에서 조건이 하나 이상 존재하고 모든 조건이 time/specific인지 확인하는 검증을 추가함.)
  * (상세: specific 시간 조건만 있는 상태에서 else action이 하나 이상 설정되면 경고 메시지를 표시하고 저장 처리를 즉시 중단하도록 구성함.)
  * (상세: 신규 등록 API와 수정 API가 모두 호출되지 않게 해 서버에 유효하지 않은 자동화 요청이 전달되지 않도록 함.)
  * (상세: 허용 범위 유지:)
  * (상세: specific 시간 조건과 센서 조건이 함께 있는 경우처럼 실제 조건 탈출 분기가 가능한 구성은 기존대로 저장할 수 있게 함.)
  * (상세: 사용자에게 특정 시간 조건만 있는 경우에는 "어떻게 종료할까요?" 동작을 설정할 수 없습니다. 메시지를 표시하도록 함.)
  * (상세: 회귀 테스트 보강:)
  * (상세: 테스트 상태에 elseActions를 반영하고 specific 조건 + 종료 동작 조합에서 경고가 발생하는지 검증함.)
  * (상세: 차단 시 postAutomations, patchAutomations가 호출되지 않는 것을 확인하는 테스트를 추가함.)
  * (상세: 커밋 및 검증 결과:)
  * (상세: c2f2759 feat: 특정 시간 동작 루틴 등록 시 else action을 등록 가능여부 검사 커밋으로 컴포넌트와 단위 테스트 변경을 반영함.)
  * (상세: 추가한 회귀 테스트 통과 및 프로덕션 빌드 성공을 확인함.)
  * (상세: 전체 AutomationBuilder 테스트에서는 현재 구현과 기존 기대값이 다른 기존 테스트 4건이 남아 있음을 확인함.)
* **[자동제어 요청·응답 로그 PK 구조 수정](https://www.notion.so/자동제어-요청응답-로그-pk-구조-수정-39ec640ccca48020853ce46521c70d05)**
  * (상세: 개요: 자동제어 in/out box 로그 테이블이 operate_id를 기본키로 사용해 동일 동작 식별자에 대한 로그 적재를 제한할 수 있는 구조를 수정함.)
  * (상세: 주요 진행 내용:)
  * (상세: 로그 식별자와 동작 식별자 역할 분리:)
  * (상세: AutomationControlRequestLog, AutomationControlResponseLog 엔티티에 자동 증가 id 기본키를 추가함.)
  * (상세: 기존 operateId는 기본키에서 일반 컬럼으로 변경해 한 동작 식별자에 연결된 요청·응답 로그를 독립 행으로 저장할 수 있게 함.)
  * (상세: 응답 로그의 nullable error_message 컬럼 정의는 프로젝트 포맷에 맞게 정리함.)
  * (상세: 커밋 반영:)
  * (상세: 0019213 fix: automation in/out box 테이블이 PK로 operate_id를 잡고있던 이슈 수정 커밋으로 요청·응답 로그 엔티티 2개 변경을 반영함.)

---

## 📝 2. 계획 대비 완료/미완료 대조 (7월 3주차 대조 표)

| 7월 3주차 계획 | 완료여부 | 7월 3주차 Daily Work Log |
| :--- | :---: | :--- |
| 📄 **[시스템 구현](https://www.notion.so/시스템-구현-368c640ccca480d2a400d2cca052ed7d) (~8/31)** | **-** | 📄 **[시스템 구현](https://www.notion.so/시스템-구현-368c640ccca480d2a400d2cca052ed7d) (~8/31)** |
| └ • 객체 추적 알고리즘 고도화 | ❌ |   |
| 📄 **[[시스템 리빌딩]웹 페이지 UI](https://www.notion.so/시스템-리빌딩웹-페이지-ui-368c640ccca480a99e43ffdc5542f3e2) (~6/5)** | **-** | 📄 **[[시스템 리빌딩]웹 페이지 UI](https://www.notion.so/시스템-리빌딩웹-페이지-ui-368c640ccca480a99e43ffdc5542f3e2) (~6/5)** |
| └ • 기기 제어 버튼 변경 | ✅ | └ • (7/13) [ByteTrack association 파이프라인 리팩터링](https://www.notion.so/bytetrack-association-파이프라인-리팩터링-39cc640ccca480b19a26e14d50f94942) |
---

## 🚀 3. 이번 주 업무 계획 (7월 4주차)

### 🔗 Task: [시스템 구현](https://www.notion.so/시스템-구현-368c640ccca480d2a400d2cca052ed7d) (~8/31)
* 노드/토픽 구성 최적화
* 진행 상황 공유 및 업무 분담
* 주행 경로 탐색 알고리즘 구상
* 3D Kalman filter 적용 분석

