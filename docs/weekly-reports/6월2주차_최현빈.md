# 📅 [6월 2주차] 주간 업무 브리핑
> 월요일 출근 시간 전, 지난주 업무 분석 및 금주 계획 종합 브리핑입니다.

---

## 📂 1. 지난주 실제 업무 보고 (Daily Work Log) (6/8~6/14)

### ■ 06월 08일 (월요일)
* **[자동화 빌더 API 예외 대응 및 미확정 수동 루틴 UI 일시 격리](https://www.notion.so/자동화-빌더-api-예외-대응-및-미확정-수동-루틴-ui-일시-격리-379c640ccca48047b935faaaf4f98244)**
  * (상세: 개요: 백엔드의 변경된 DTO 구조와 API 명세에 맞춰 자동화 저장 요청 시 발생하던 오류 필드를 보정하고, 기획 사양이 미확정된 기능의 인터페이스를 숨김 처리함.)
  * (상세: 주요 진행 내용:)
  * (상세: 자동화 명세 수정 시 API Unexpected Error 대응 ([automationStore.ts] [AutomationBuilder.tsx]))
  * (상세: [automationStore.ts] patchAutomations 비동기 액션에서 API 요청 페이로드 바디에 불필요하게 id가 포함되어 백엔드 유효성 검사 에러를 야기하던 문제를 해결하기 위해, 요청 전 id 프로퍼티를 명시적으로 제거한 뒤 엔드포인트 세그먼트에만 사용하도록 조치함.)
  * (상세: [AutomationPage.tsx] 백엔드에서 전달받은 규칙 내에 every 스케줄이 정의되어 있을 때, 빌더 및 목록 렌더링 시 발생 가능한 타입 불일치 에러를 방지하고자 '자동 실행' 조건 상태로 정상 변환(디코딩)하여 처리하는 가드를 추가함.)
  * (상세: 수동 루틴 기능 및 UI 일시 격리 ([AutomationBuilder.tsx] [RoutineCard.tsx]))
  * (상세: 백엔드 지원 스펙이 확정되지 않은 '다른 루틴 실행하기(수동 루틴 연동)' UI 컴포넌트와 액션 추가 버튼을 빌더에서 임시 주석 처리하고 비활성화함.)
  * (상세: [RoutineCard.tsx] 수동 모드일 때 알림창을 띄우며 중단하던 기존 로직 대신, 사용자 인터랙션이 막힘없이 실행될 수 있도록 클릭 핸들러의 분기 필터를 제거하여 일관된 토글 흐름으로 정비함.)
* **[RabbitMQ 메시지 최상위 공통 필드 마이그레이션 및 RPC Ack 전송 로직 탑재](https://www.notion.so/rabbitmq-메시지-최상위-공통-필드-마이그레이션-및-rpc-ack-전송-로직-탑재-379c640ccca480e8b0b5fcede47b4614)**
  * (상세: 개요: 수신 큐 메시지 규격을 전면 조율하여 중복 필드를 정리하고, PLC 테스터 내부에서 RPC(Request-Response) 패턴을 지원하기 위해 reply_to 및 correlation_id를 활용한 응답 송신 메커니즘을 구축함.)
  * (상세: 주요 진행 내용:)
  * (상세: 메시지 DTO 필드 최상위 이양 ([models.py])
  * (상세: 개별 DTO(Payload, AutomationPayload) 하위에 각각 정의되어 데이터 중복 및 모델 파싱 복잡도를 유발하던 operate_id와 timestamp 필드를 최상위 AMQPReceiveMessage 레벨로 끌어올려 단일화함.)
  * (상세: 비동기 RPC Ack 응답 구현 ([consumer.py])
  * (상세: 백엔드의 RPC 요청에 대응하여 ControlEventType.CONTROL_AUTOMATION 및 ControlEventType.CONTROL_CONTACT 수신 처리 시, 메시지 컨텍스트의 reply_to 큐와 correlation_id가 지정되어 있는 경우 ACK_CONTACT 이벤트를 담은 성공/실패 응답 페이로드를 발행하는 RPC 피드백 로직을 구현함.)
  * (상세: 가상 주소 변환 코드 정비 ([automation/scheduler.py])
  * (상세: 기기 가상 맵핑 테이블(device_mappings)을 거쳐 주소를 변환하던 _resolve_address 로직을 정리하고, 전달받은 접점의 원본 address를 그대로 가상 PLC 디렉토리에 바인딩하도록 단순화하여 실 접점 제어 흐름과 매칭도를 향상시킴.)
* **[RabbitMQ 메시지 발행 구조 개선 및 중복 Ack 로깅·예외 처리 로직 서비스 추상화](https://www.notion.so/rabbitmq-메시지-발행-구조-개선-및-중복-ack-로깅예외-처리-로직-서비스-추상화-379c640ccca48074af06f98313bcf65c)**
  * (상세: 개요: API 컨트롤러에서 RabbitMQ 메시지를 발행할 때 서비스 레이어의 간섭을 최소화하여 직접 발행하도록 구조를 조정하고, 여러 서비스에 산재해 있던 중복 Ack 수신 및 에러 로깅/예외 처리 코드를 [RabbitMQService] 내로 공통화 및 추상화함.)
  * (상세: 주요 진행 내용:)
  * (상세: 직접 발행 구조로의 메시지 발행 레이어 개선:)
  * (상세: 컨트롤러가 직접 메시지를 발행하도록 호출 구조를 단순화하고 결합도를 낮춤.)
  * (상세: Ack 응답 처리 공통 서비스 추상화)
  * (상세: 기존에 [AutomationService] 양측에 개별적으로 작성되어 중복을 유발하던 amqpConn.request 호출, LogStateType.ERROR 검증, Ack 성공 로그 저장(saveAckLog), 타임아웃 예외 처리(saveErrorLog 및 TimeoutError 예외) 로직을 메서드로 공통 래핑하여 리팩토링함.)
  * (상세: [rabbitmq.service.ts] AMQPReceiveMessage의 구조 변경에 맞추어 plcId를 추출할 때 메시지 타입(접점 제어 'contact' / 자동화 규칙 'automation')에 따라 적절한 DTO 페이로드에서 plcId를 바인딩할 수 있도록 type 파라미터를 추가하고 분기 처리 로직을 적용함.)

### ■ 06월 09일 (화요일)
* **[순환 참조 제거, RuleScheduler 캡슐화 및 주기적 조건 실행 대기 메커니즘 구축](https://www.notion.so/순환-참조-제거-rulescheduler-캡슐화-및-주기적-조건-실행-대기-메커니즘-구축-37ac640ccca480d58394ef5fd97bf03d)**
  * (상세: 개요: Python 모듈 설계 과정에서 결합도가 높은 클래스들 사이의 순환 참조 문제를 절대 경로 기반 임포트로 해소하고, 주기적인 제어 구문(EveryAction)에 대해 특정 시각과의 시간차만큼 비동기 대기 후 구동되도록 지연 실행 알고리즘을 도입함.)
  * (상세: 주요 진행 내용:)
  * (상세: 순환 참조 임포트 에러 해결 (__init__.py):)
  * (상세: app.core.module, app.service.handler, app.service.rabbitMQ 등의 모듈이 서로 엮이던 상대 경로를 패키지 절대 경로 명세로 전환하고, __init__.py 등에서 의존성 로딩 시점을 분리하여 시스템 구동 시 비정상 종료되던 순환 참조 문제를 완결함.)
  * (상세: RuleScheduler 정보 은닉 및 컨텍스트 위임 (scheduler.py):)
  * (상세: 테스터와 동일한 아키처 규칙에 따라, PLC 서버 내에서도 RuleExecutionContext에 주기 태스크를 위임 등록하도록 register_periodic_action API를 설계하고, models.py에서 스케줄러에 직접 조작을 가하지 못하게 래핑 차단함.)
  * (상세: 시점 피연산자를 활용한 주기 실행 대기 로직 개발 (models.py, parser.py):)
  * (상세: models.py의 EveryAction 모델이 인스턴스화될 때 specific_operand 정보를 추가로 할당받아, 구동 직전 기준 시간 타임스탬프와 현재 로컬 시간의 잔여 격차를 연산(get_sleep_time)하고 그 격차만큼 asyncio.sleep 비동기 대기를 수행한 후 주기적 루프 태스크에 등록될 수 있도록 고도화함.)
  * (상세: parser.py: AST 규칙 디코딩 시 피연산자의 specific 및 time 키워드가 올바르게 DatetimeOperand 모델로 파싱되어 들어올 수 있도록 RuleParser.parse_operand 분기 조건을 확장함.)
* **[수동 제어 루틴의 목업(Mock) 제거 및 실제 API 인터랙션 연동](https://www.notion.so/수동-제어-루틴의-목업mock-제거-및-실제-api-인터랙션-연동-37ac640ccca48082b84af6943f76f27e)**
  * (상세: 개요: 미완성 사양으로 격리해두었던 수동 제어 루틴 카드의 동작 메커니즘을 백엔드 API 연동과 최종 결합하여 모킹 데이터를 제거하고 일관된 제어 인터랙션으로 개선함.)
  * (상세: 주요 진행 내용:)
  * (상세: 실제 API 호출 바인딩 및 모킹 얼럿 제거:)
  * (상세: RoutineCard.tsx: 수동 모드(isManual)인 경우 단순 경고창(alert)을 띄우며 제어를 중단하던 모킹 핸들러 로직을 걷어내고, 수동 루틴 버튼 클릭 시에도 백엔드로 토글 명령이 직접 트리거될 수 있도록 API 바인딩인 onToggle(routine.id) 공통 동작을 연결하여 실제 동작을 제어할 수 있도록 보완함.)
* **[단일 디바이스 제어 수신 필드 DTO 마이그레이션 및 규칙 스케줄러 캡슐화 고도화](https://www.notion.so/단일-디바이스-제어-수신-필드-dto-마이그레이션-및-규칙-스케줄러-캡슐화-고도화-37ac640ccca480c9881ce2a8263c23a6)**
  * (상세: 개요: 단일 디바이스 제어 명령 수신 시 AMQP DTO 필드의 최상위 구조 이양에 따른 검증 및 테스트를 반영하고, 규칙 스케줄러가 외부 객체에서 직접 접근되지 않도록 컨텍스트를 활용한 캡슐화 정비 및 주기적 조건 실행 구조를 개선함.)
  * (상세: 주요 진행 내용:)
  * (상세: 수신 큐 메시지 규격 검증 및 테스트 코드 보정 (consumer.py, models.py):)
  * (상세: AMQPReceiveMessage DTO가 기존 dataPayload 내에 존재하던 operateId와 timestamp를 최상위 공통 필드 구조로 마이그레이션한 것을 테스트 코드에도 전면 반영하여 test_consumer.py 및 test_models.py의 검증 시나리오를 정상화함.)
  * (상세: RuleScheduler에 대한 직접 제어 차단 및 캡슐화 정밀화 (scheduler.py):)
  * (상세: 개별 실행 액션(models.py)이 context.scheduler 필드를 통해 직접 스케줄러에 접근하여 상태를 조회하고 등록하던 형태를 지양하기 위해, RuleExecutionContext 상에 is_periodic_action_active 및 register_periodic_action 래퍼 메서드를 구현하여 스케줄러를 감춤.)
  * (상세: 주기 실행 규칙 파싱 및 스케줄러 취소 정비 (parser.py, consumer.py):)
  * (상세: RuleParser에서 주기 조건 구문 파싱 시 요일/시간 등 동적 조건 판단용 specific 피연산자가 포함되었을 경우, 이를 정상 파싱하여 specific_operand로 EveryAction에 바인딩할 수 있도록 구문을 정비함.)
  * (상세: consumer.py에서 새로운 자동화 활성화 수신 시, 이전과 동일한 규칙의 구동 태스크 취소 위치를 조율하여 태스크가 누락 없이 정리될 수 있도록 변경함.)
* **[자동화 수정 및 활성화/비활성화 시 Ack 수신 데이터베이스 상태 저장 누락 복구](https://www.notion.so/자동화-수정-및-활성화비활성화-시-ack-수신-데이터베이스-상태-저장-누락-복구-37ac640ccca48068a745f82a6564a0a1)**
  * (상세: 개요: 백엔드 API에서 자동화 규칙을 수정하거나 활성화/비활성화 상태를 토글할 때, RabbitMQ로 제어 메시지를 송신하여 PLC 및 테스터로부터 Ack 응답을 정상 수신했음에도 불구하고 변경된 자동화 정보를 최종 데이터베이스에 반영(저장)하는 로직이 누락된 문제를 파악하여 복구하고, Ack 응답 처리 시 DTO 형식에 따른 PLC ID 추출 분기 처리를 정비함.)
  * (상세: 주요 진행 내용:)
  * (상세: 데이터베이스 상태 동기화 누락 수정:)
  * (상세: automation.service.ts: update 및 toggle 수행 후 rabbitService.ack_response 호출이 정상적으로 처리(Ack 수신 완료)되면, 누락되었던 데이터베이스 상태 업데이트 코드 await this.repository.upsert(automation);를 추가하여 백엔드 DB와 실제 적용 사양이 동기화되도록 조치함.)
  * (상세: Ack 응답 처리 파라미터 보정 및 PLC ID 바인딩 개선:)
  * (상세: device.service.ts: rabbitmqService.ack_response 호출부에 'contact' 타입을 매개변수로 명시하여 DTO 분기가 알맞게 작동하도록 대응함.)
  * (상세: rabbitmq.service.ts: ack_response 메서드의 파라미터에 type('contact' | 'automation')을 추가하고, 타입 정보에 따라 메시지 구조의 automationPayload 혹은 dataPayload로부터 각각 알맞은 plcId를 바인딩하여 saveAckLog를 신뢰성 있게 호출하도록 리팩토링함.)

### ■ 06월 11일 (목요일)
* **[자동화 루틴 영속 백업 구축, 가변 대기 스케줄링 구현 및 레거시 제거](https://www.notion.so/자동화-루틴-영속-백업-구축-가변-대기-스케줄링-구현-및-레거시-제거-37cc640ccca480c8905edaa1a330abd6)**
  * (상세: 개요: 자동화 구동 중인 상태의 유실 방지를 위해 SQLite를 활용한 영속화와 서버 재부팅 시 자동 로드(Reload) 메커니즘을 적용하고, 가변적 시각 대기가 가능한 스케줄러 기능 개발 및 대규모 레거시 청소를 완료함.)
  * (상세: 주요 진행 내용:)
  * (상세: 로컬 DB 백업 기능과 서버 기동 시 자동화 루틴 자동 로드 연동:)
  * (상세: app/repository/automation_repository.py: AMQP 수신 메시지를 기준으로 자동화 상태를 데이터베이스에 지속적으로 upsert하여 규칙들을 백업 보존하도록 영속 구조를 구축함.)
  * (상세: app/service/handler.py: AutomationControlHandler가 가동 중인 런타임 스케줄러 상에 새 규칙을 등록(RuleEngine)하거나 취소하기 전에, save_automation을 트리거하여 최종 활성화 상태가 SQLite DB에 최신 반영되도록 연동함.)
  * (상세: main.py: PLC 서버 구동 라이프사이클을 setup_configs, setup_db, setup_services, setup_automation 등으로 순차 분할 설계함. 특히 setup_automation()을 통해 부트스트랩 단계에서 SQLite 로컬 DB 내 is_active=True 상태의 규칙 목록을 조회하여, 런타임에 직접 ControlEventType.CONTROL_AUTOMATION 제어 이벤트 핸들러를 가동함으로써 서버 재부팅 시에도 유실 없이 자동으로 기존 구동 중이던 모든 예약 태스크가 스케줄러에 로드되도록 복원 메커니즘을 확보함.)
  * (상세: 특정 요일 및 가변 시각 대기 스케줄러 개발:)
  * (상세: app/service/automation/scheduler.py: 특정 요일 및 시각(예: 매주 월요일 09:00 등) 정보를 분석하여 다음 기동 예상 시각을 연산하는 calculate_next_trigger_time을 구현하고, 기동 시각과 현재 시각의 차이만큼 비동기 대기(asyncio.sleep) 시킨 뒤 액션을 수행하게 하는 register_specific 비동기 예약 루프 함수를 개발함.)
  * (상세: app/service/automation/models.py: EveryAction.execute 내부에서 specific 구성이 설정된 경우 기존 1초/1분 등 간격 주기 대신 context.scheduler.register_specific을 호출하도록 실행 경로 분기를 구축함.)
  * (상세: 레거시 옵저버 및 로거 퍼블리셔 제거 등 복잡도 최적화:)
  * (상세: app/implements/observer.py, app/service/action_service.py 삭제: 과거에 사용했던 센서 측정 통지용 SensorEvaluationSubject/SensorObserver 객체 및 시간 주기 액션 관리 ActionTimer/ActionTrigger 등의 모듈들을 전체 청소하여 구조적 결합을 파괴하고 단순화함.)
  * (상세: app/utils/logger.py: RabbitMQ 메시지 브로커로 실시간 로그를 큐잉하던 AsyncRabbitMQLogHandler를 삭제하고 퍼블리셔 의존성이 배제된 콘솔 로컬 로깅으로 슬림화하여, 로깅 단계에서 생기던 복잡한 순환 예외와 자원 누수를 해결함.)
  * (상세: app/interfaces/type.py: 복잡한 Contact, Register 데이터클래스 직렬화 함수를 딕셔너리 직접 전달 방식으로 단순화하여 DTO 경량화를 완료함.)
  * (상세: 주소 직접 참조에 따른 단위 테스트 보정:)
  * (상세: tests/app/service/automation/test_engine.py, test_parser.py, test_scheduler.py: 기존에 테스트 픽스처 MockContext에서 동작하던 장치 주소 가상 매핑(DEV1 -> D100)을 폐지하고, DeviceOperand 및 CommandAction이 PLC의 실제 하드웨어 주소를 직통 참조하도록 수정한 최신 엔진 사양에 맞추어 모든 모킹 테스트와 단언(assert) 경로를 정상 보완함.)
* **[자동화 루틴 디코더 공통 유틸 위임 및 자동 새로고침 연동](https://www.notion.so/자동화-루틴-디코더-공통-유틸-위임-및-자동-새로고침-연동-37cc640ccca4809f8470ddf88a936bc5)**
  * (상세: 개요: 페이지 내의 복잡한 자동화 AST 룰 파싱 로직을 공통 유틸 함수로 이관하여 격리하고, 루틴 등록/수정 동작 후 목록이 연동되어 자동으로 갱신될 수 있도록 리팩토링 및 검증 테스트를 보완함.)
  * (상세: 주요 진행 내용:)
  * (상세: AST 룰 파싱 로직 분리 및 공통 디코더 모듈화:)
  * (상세: src/utils/automationMapper.ts: 기존 AutomationPage.tsx 내부에서 렌더링을 위해 수행하던 복잡한 AutomationRule AST 분기 처리 및 텍스트 포맷팅("매주 월, 화 09:00~18:00에 실행" 등)을 전면 이관해 decodeAutomationRule 공통 모듈로 설계함. offset.value 부재 시 00:00 처리, contacts/registers 미매칭 시 기본 명칭("센서", "기기") 폴백 등 안전한 엣지 케이스 처리 로직을 구현함.)
  * (상세: tests/page/AutomationPage.test.tsx: 다중 시간 조건 결합, 언디파인드 컨디션 처리, 장비 주소 부재 상황 등 6개 이상의 엣지 케이스 시나리오를 세분화하여 복합적으로 룰 해독 무결성을 보증하는 단위 테스트 스위트를 구축함.)
  * (상세: 루틴 데이터 갱신 후 화면 자동 새로고침 바인딩:)
  * (상세: src/store/automationStore.ts: 자동화 규칙 생성(postAutomations) 및 일부 필드 변경(patchAutomations) 비동기 액션이 정상 완료(status 200)되면, 내부적으로 즉각 fetchAutomations 목록 조회 액션을 강제 호출하여 화면 새로고침 없이 바인딩된 상태 데이터가 실시간 새로고침되도록 플로우를 개선함.)
  * (상세: src/page/AutomationPage.tsx: 페이지 컴포넌트 내의 복잡한 임포트 로직을 줄이고, UI 렌더링 시 decodeAutomationRule 유틸을 활용해 데이터 모델을 렌더링하도록 뷰 코드를 정리함.)
  * (상세: 임포트 에러 수정 및 타입 경로 단일화:)
  * (상세: src/components/automation/AddCategoryOverlay.tsx, RoutineCard.tsx, RoutineDetailOverlay.tsx: Routine 인터페이스의 수입처를 AutomationPage에서 글로벌 types/index.ts로 통일하여 참조 구조의 컴파일 에러를 수정함.)
* **[순환 참조 제거를 위한 캡슐화 정비 및 가상 PLC 연동 초안 구현](https://www.notion.so/순환-참조-제거를-위한-캡슐화-정비-및-가상-plc-연동-초안-구현-37cc640ccca480e39b9ec22faa7aea64)**
  * (상세: 개요: EveryAction과 RuleScheduler 간의 순환 의존성(Circular Dependency) 해소를 위해 컨텍스트에 스케줄러 등록 기능을 래핑하고, 백엔드 서버의 PLC 초기화 API를 호출해 가상 PLC의 장치 사양을 세팅하는 인터랙션과 구조화된 로깅을 추가함.)
  * (상세: 주요 진행 내용:)
  * (상세: 의존성 캡슐화 위임 및 스케줄러 작동 단위 테스트 구축:)
  * (상세: automation/scheduler.py: RuleExecutionContext 클래스 내에 is_periodic_action_active 및 register_periodic_action 대리 메소드를 구현하여 내부 스케줄러(RuleScheduler)를 직접 노출시키지 않도록 정보를 은닉함.)
  * (상세: automation/models.py: EveryAction.execute가 스케줄러 객체 대신 context가 제공하는 대리 인터페이스를 거치도록 수정하여 순환 참조 결합 고리를 차단함.)
  * (상세: tests/automation/test_scheduler.py: EveryAction 실행 시 RuleExecutionContext를 거쳐 RuleScheduler에 비동기 루프 태스크가 정상 등록되고, 1.2초 대기하여 1초 주기의 가상 PLC QX0.1 접점 제어가 정상 수행되는지 및 태스크 취소 기능이 원활한지 단언하는 신규 단위 테스트를 도입함.)
  * (상세: 실제 API 기반 장치 초기화 연동 및 로깅 개선:)
  * (상세: main.py: pyhumps 및 requests 모듈을 도입하여, 구동 시 로컬 백엔드 서버의 PLC 초기화 API(http://localhost:8443/api/v1/plc/init/1)를 동기식 호출하고, 카멜케이스 JSON 사양을 humps.decamelize를 통해 스네이크케이스 DTO 데이터로 매핑하여 가상 PLC 내 디바이스(contacts, registers) 사양을 초기 출력 및 반영할 수 있는 초안 연동을 추가함.)
  * (상세: logger.py: structlog와 Python 기본 로깅 핸들러를 결합하여 콘솔 상의 구조화된 컬러 렌더링을 지원하는 setup_logging 초기화 함수를 신설 및 main.py에 바인딩함.)
  * (상세: pyproject.toml, uv.lock: pyhumps, requests, structlog 라이브러리를 의존성에 추가함.)
* **[모니터링 Loki gRPC 연결 오류 해결](https://www.notion.so/모니터링-loki-grpc-연결-오류-해결-37cc640ccca480edbb28f2a7b623c78b)**
  * (상세: 개요: Loki 3.x 구동 시 내부 컴포넌트 간 gRPC 다이얼 타임아웃 장애 원인을 분석해 해결 및 트러블슈팅 가이드를 추가함.)
  * (상세: 주요 진행 내용:)
  * (상세: Loki 스케줄러 gRPC 타임아웃 장애 조치 및 모니터링 안정화:)
  * (상세: docker/monitoring/loki-config.yaml: Loki 내부 컴포넌트 간 통신 주소(instance_addr)가 컨테이너 외부 IP로 자동 감지되어 통신 시 타임아웃(i/o timeout)이 발생하는 현상을 방지하기 위해, common 설정에 instance_addr: 127.0.0.1 설정을 명시하여 내부 gRPC 멤버십 통신을 로컬 루프백 내부로 격리함.)
  * (상세: docs/troubleshooting/2026-06-11-loki-scheduler-timeout.md: Loki 3.x 버전의 아키처 특성에 따른 장애 원인 분석 내용, 조치용 설정 Diff 및 컨테이너 재가동을 통한 결과 검증 방안이 포함된 상세 장애 대비 매뉴얼을 추가함.)
* **[PLC 초기화 API 최적화](https://www.notion.so/plc-초기화-api-최적화-37cc640ccca48061b5c6f81b5b85d164)**
  * (상세: 개요: PLC 초기화 시 연동 장치 상세 정보 조회 쿼리를 단일 Inner Join으로 최적화하여 성능 및 구조를 정비)
  * (상세: 주요 진행 내용:)
  * (상세: PLC 초기화 조회 쿼리 최적화 및 단일화:)
  * (상세: src/modules/api/plc/repository/plc.repository.ts: 기존에 registers, contacts, sensors 세 가지 테이블을 각각 쿼리 빌더로 다중 조인하여 병합하던 복잡한 findAllObserveElements 메소드를 제거하고, plc.devices 테이블과 단 한번 조인하여 데이터를 가져오는 findAllDevicesById 메소드로 전환하여 데이터베이스 접근 효율성을 대폭 개선함.)
  * (상세: src/modules/api/plc/service/plc.service.ts: initPLC 서비스 내부에서 여러 개의 개별 레포지토리 의존성 호출부를 걷어내고, 레포지토리에서 조회한 디바이스 배열에서 DeviceEntityType(REGISTER, CONTACT)에 따라 filter하여 분류하고 병합 반환하도록 구조를 경량화함.)
  * (상세: src/modules/api/plc/plc.module.ts: 불필요한 ContactModule 및 RegisterModule 임포트 의존성을 해제함.)

### ■ 06월 12일 (금요일)
* **[자동화 비활성화 시 영속 데이터 완전 삭제 구현 및 에러 핸들링 보완](https://www.notion.so/자동화-비활성화-시-영속-데이터-완전-삭제-구현-및-에러-핸들링-보완-37dc640ccca480b29f1cf3d50f6b6565)**
  * (상세: 개요: 자동화 규칙 관리 시 비활성화(deactivate) 요청을 영속 디스크인 SQLite DB에서도 완전 삭제(DELETE)하도록 쿼리 및 제어 흐름을 수정하고, 에러 통신 발생 시 누락된 실패 지시 상태값(ERROR)을 명시하여 시스템 연동 무결성을 높임.)
  * (상세: 주요 진행 내용:)
  * (상세: 자동화 비활성화 규칙 로컬 DB 완전 삭제(DELETE)로 수정:)
  * (상세: app/repository/automation_repository.py: 기존에 비활성화 시 is_active 플래그를 False로 단순히 업데이트하던 deactivate 메서드를 삭제하고, 데이터베이스에서 레코드를 아예 제거하는 SQL DELETE FROM automation WHERE id = ? 쿼리 기반의 delete 메서드로 대체함.)
  * (상세: app/service/handler.py: AutomationControlHandler.process 메서드에서 자동화 요청을 처리할 때 기존 무조건 upsert 처리하던 공통 흐름을 제거하고, 요청의 is_active 상태가 참일 때만 DB에 규칙을 저장/수정(upsert)하도록 분기를 쪼갬. 만약 비활성화(False) 상태로 수신된 경우 self._repository.delete를 직통 호출하여 SQLite에서 해당 데이터를 삭제 처리함. 또한 상태 응답 메시지의 대상 표시 명칭을 기존 operate_id에서 사람이 읽기 쉬운 자동화 규칙명(self._payload.name)으로 보완함.)
  * (상세: 통신 장애 응답 상태 정의 보완 및 복구:)
  * (상세: app/interfaces/type.py: 에러 전파 시 사용되던 StatusType 내 실패의 상태 문자열 값을 FAILURE에서 타 컴포넌트와의 호환을 위해 ERROR로 변경 정의함.)
  * (상세: app/service/handler.py: AutomationControlHandler 구동 중 비동기 스케줄러 처리 혹은 룰 파싱 실패 시, 예외 블록(except)에서 응답 객체에 StatusType.ERROR.value가 정확하게 실려 반환되도록 에러 식별 상태 코드 지정을 복구함.)
* **[API 기반 PLC 초기화 연동 및 구조화된 로깅 모듈 신설](https://www.notion.so/api-기반-plc-초기화-연동-및-구조화된-로깅-모듈-신설-37dc640ccca480049cbbc549841f03ba)**
  * (상세: 개요: 가상 PLC 기동 시 백엔드의 장치 설정 초기화 API 데이터를 직접 로드하여 런타임 가상 접점 사양을 맵핑하도록 연동하고, 구조화되고 가독성이 높은 로그 출력을 위해 structlog 전용 모듈을 구축함. (현재 로컬 작업 중인 미커밋 변경 사항))
  * (상세: 주요 진행 내용:)
  * (상세: 실제 API 데이터 기반 가상 PLC 하드웨어 구성 연동:)
  * (상세: main.py: 기동 시 requests.get('http://localhost:8443/api/v1/plc/init/1')을 동기적으로 호출하여 백엔드에 정의된 1번 스테이션의 사양을 획득함. 반환된 카멜케이스 JSON 데이터를 pyhumps 라이브러리의 humps.decamelize를 활용해 스네이크케이스 딕셔너리로 일괄 변환하여 가상 PLC 내 contacts 및 registers 초기 할당에 활용하는 연동 코드를 추가함.)
  * (상세: structlog 결합 구조의 포맷 로거 구현:)
  * (상세: logger.py (신규 파일): Python 표준 logging과 structlog를 융합하여 시간, 로그 레벨, 로거명 및 가독성을 살린 컬러풀한 콘솔 렌더러(ConsoleRenderer) 설정을 포함하는 setup_logging 초기화 모듈을 신설함.)
  * (상세: main.py: 프로그램 실행 진입점에서 setup_logging()을 실행하도록 하여 전체 로깅 수준을 통일함.)
  * (상세: pyproject.toml, uv.lock: 신규 도입한 pyhumps, requests, structlog 패키지 의존성을 명시하고 락파일을 업데이트함.)
* **[RabbitMQ Ack 방식 롤백, 상태 로그 저장 표준화 및 InfluxDB 실제 데이터 연동](https://www.notion.so/rabbitmq-ack-방식-롤백-상태-로그-저장-표준화-및-influxdb-실제-데이터-연동-37dc640ccca4801b9a0cd6ec234b98f1)**
  * (상세: 개요: RabbitMQ 제어 시 동기식 요청-응답 형태를 RxJS Observable 기반의 비동기식 모니터계로 원복하고, 요청/응답/실패 각 라이프사이클의 로깅을 서비스단으로 이관해 표준화했으며, InfluxDB 레지스터 상세 정보 조회 시 목업을 걷어내고 실측 데이터를 쿼리해 오도록 연동함.)
  * (상세: 주요 진행 내용:)
  * (상세: 비동기 Ack 응답 모니터링 방식 롤백 및 결합:)
  * (상세: src/modules/rabbitmq/rabbitmq.service.ts: amqpConn.request를 사용하던 동기 방식 ack_response 메서드를 삭제하고, publish로 이벤트를 전송한 후 응답 브로커에서 operateId에 매칭되는 Ack 메시지를 RxJS Observable을 생성해 대기 및 타임아웃 처리하도록 한 기존의 createObservable 및 publish 구조로 복원함.)
  * (상세: src/modules/api/automation/automation.service.ts, src/modules/api/device/device.service.ts: 서비스 메서드들의 결과 반환 유형을 Promise<Observable<AMQPAckMessage>>로 원복하고 그에 맞추어 제어 발송 로직을 정비함.)
  * (상세: 수정된 서비스 단 중심의 요청/응답 제어 로그 영속화:)
  * (상세: src/interceptor/rabbitmq-logging.interceptor.ts 삭제: 이전의 전역 인터셉터 방식의 로깅 체계를 걷어내고 보다 정밀한 로깅 제어를 가능하게 함.)
  * (상세: src/modules/api/log/dto/create-log.dto.ts: 로그 인스턴스를 요청 라이프사이클에 맞춰 안전하게 생성할 수 있도록 createRequestLog, createResponseLog, createErrorLog 등의 정적 팩토리 메서드를 추가함.)
  * (상세: src/modules/rabbitmq/rabbitmq.service.ts: 메시지 발행(publish), 성공적인 Ack 응답 수신(createObservable 내 concatMap), 통신 실패 혹은 예외(catchError) 시 해당 시점에 최적화된 CreateLogDto 팩토리 메서드를 거쳐 logService.saveLog에 로그가 즉각 기록되도록 보완함.)
  * (상세: src/modules/api/log/log.service.ts, log.entity.ts, types.ts: LogType 구조를 REQUEST, RESPONSE로 단순화하고, HMI와 WEB을 구분 짓는 OperateType을 새롭게 추가하여 데이터베이스 엔터티와 DTO 필드를 정비함.)
  * (상세: InfluxDB 레지스터 실측 데이터 연동 및 조회 기능 개선:)
  * (상세: src/modules/api/device/device.service.ts: populateDetails 내에서 레지스터 타입의 장치인 경우, 기존의 가짜 목업 시계열 데이터 대신 InfluxDB Client를 통해 최근 6시간 동안의 실제 데이터를 1분 평균 단위로 조회하여 차트용 시계열 배열(detail.chart)을 빌드하게 함.)
  * (상세: src/modules/api/register/register.dto.ts, src/modules/db/influx/types.ts: 고정 interval 조회 매개변수를 완전히 삭제하고, 정밀 분할 조회를 위한 window 객체(WindowFn) 관련 클래스 구조 및 집계 함수 형식(AggregateFn)을 DTO와 도메인 타입에 추가함.)
  * (상세: src/modules/db/influx/client.ts, queryBuilder.ts: InfluxDB Flux 쿼리 빌더가 WindowFn 스펙을 해석하여 동적으로 Flux 쿼리문을 만들고 행 데이터를 모을 수 있도록 보완함.)

---

## 📝 2. 계획 대비 완료/미완료 대조 (6월 2주차 대조 표)

| 6월 2주차 계획 | 완료여부 | 6월 2주차 Daily Work Log |
| :--- | :---: | :--- |
| 📄 **[[시스템 리빌딩]PLC-Server 재설계](https://www.notion.so/시스템-리빌딩plc-server-재설계-368c640ccca4804e9343e7e83764de0e) (~6/26)** | **-** | 📄 **[[시스템 리빌딩]PLC-Server 재설계](https://www.notion.so/시스템-리빌딩plc-server-재설계-368c640ccca4804e9343e7e83764de0e) (~6/26)** |
| └ • 테스트 동작 기능 추가 | ✅ | └ • (6/9) [순환 참조 제거, RuleScheduler 캡슐화 및 주기적 조건 실행 대기 메커니즘 구축](https://www.notion.so/순환-참조-제거-rulescheduler-캡슐화-및-주기적-조건-실행-대기-메커니즘-구축-37ac640ccca480d58394ef5fd97bf03d) |
| └ • 수동 실행 기능 추가 | ✅ | └ • (6/8) [자동화 빌더 API 예외 대응 및 미확정 수동 루틴 UI 일시 격리](https://www.notion.so/자동화-빌더-api-예외-대응-및-미확정-수동-루틴-ui-일시-격리-379c640ccca48047b935faaaf4f98244) |
| 📄 **[[시스템 리빌딩]인프라 재구축](https://www.notion.so/시스템-리빌딩인프라-재구축-36fc640ccca480db804dd69c7ff3d9fb) (~6/19)** | **-** | 📄 **[[시스템 리빌딩]인프라 재구축](https://www.notion.so/시스템-리빌딩인프라-재구축-36fc640ccca480db804dd69c7ff3d9fb) (~6/19)** |
| └ • 수동 실행 규격 추가 | ✅ | └ • (6/8) [자동화 빌더 API 예외 대응 및 미확정 수동 루틴 UI 일시 격리](https://www.notion.so/자동화-빌더-api-예외-대응-및-미확정-수동-루틴-ui-일시-격리-379c640ccca48047b935faaaf4f98244) |
| └ • 테스트 동작 규격 추가 | ✅ | └ • (6/9) [순환 참조 제거, RuleScheduler 캡슐화 및 주기적 조건 실행 대기 메커니즘 구축](https://www.notion.so/순환-참조-제거-rulescheduler-캡슐화-및-주기적-조건-실행-대기-메커니즘-구축-37ac640ccca480d58394ef5fd97bf03d) |
| 📄 **[[시스템 리빌딩]벡엔드 DB, API 추가/변경](https://www.notion.so/시스템-리빌딩벡엔드-db-api-추가변경-368c640ccca480409db3c195b9ad977f) (~6/12)** | **-** | 📄 **[[시스템 리빌딩]벡엔드 DB, API 추가/변경](https://www.notion.so/시스템-리빌딩벡엔드-db-api-추가변경-368c640ccca480409db3c195b9ad977f) (~6/12)** |
| └ • Redis 캐싱 전략 수정 | ❌ |   |
---

## 🚀 3. 이번 주 업무 계획 (6월 3주차)

### 🔗 Task: [[시스템 리빌딩]벡엔드 DB, API 추가/변경](https://www.notion.so/시스템-리빌딩벡엔드-db-api-추가변경-368c640ccca480409db3c195b9ad977f) (~6/12)
* 접점 변경 로그 DB 수정
* 테스트 동작 API 추가

### 🔗 Task: [[시스템 리빌딩]통신 프로토콜 재설계](https://www.notion.so/시스템-리빌딩통신-프로토콜-재설계-36ec640ccca480e7922fc5cf72694fa4) (~6/19)
* 접점/레지스터 변경 사항 송신 스키마 변경
* 테스트 동작 요청 스키마 구현

### 🔗 Task: [[시스템 리빌딩]인프라 재구축](https://www.notion.so/시스템-리빌딩인프라-재구축-36fc640ccca480db804dd69c7ff3d9fb) (~6/19)
* 백/프론트/plc 배포

### 🔗 Task: [[시스템 리빌딩]웹 페이지 API 연동](https://www.notion.so/시스템-리빌딩웹-페이지-api-연동-368c640ccca480dcbce8ebeb10e0464d) (~6/12)
* 접점 수동 조작 API 변경사항 적용

